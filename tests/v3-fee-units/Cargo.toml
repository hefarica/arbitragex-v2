[package]
name = "arbx-v3-fee-units-tests"
version = "0.0.0"
edition = "2021"
publish = false

# Compile the actual production module, never a rewritten test copy.
[lib]
path = "../../backend/searcher-rs/src/pool_discovery/v3_fee.rs"

[workspace]

[dependencies]
serde = { version = "=1.0.229", features = ["derive"] }
serde_json = "=1.0.151"
