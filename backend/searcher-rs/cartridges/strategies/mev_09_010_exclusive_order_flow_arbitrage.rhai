// MEV-09-010 — Exclusive-order-flow arbitrage
// Detector family: I_ORDERFLOW (AUTHORIZED_FLOW_ONLY)
// Generated by scripts/generate_cartridges_v3.py from the canonical Excel
// (02_CARTRIDGE_MATH_MAP). Universal Cartridge Contract v3.
//
// This detector requires AUTHORIZED_FLOW_ONLY data/settlement that is not yet wired to
// a real binding. Per RULE 00 / R8 it is OBSERVE-ONLY: is_opportunity stays
// false with an explicit reason. It NEVER fabricates a profit number.
// Detector equation (reference, evaluated downstream once data exists):
//   Only evaluate flows explicitly available to this searcher; price the authorized order/bundle against alternative settlement and costs. No inference of privileged/private data.
// Primary operators (evidence only): [25, 27, 24]   Secondary: [11, 22, 23]
//
// NOTE: runner invokes functions with an EMPTY scope -> global consts are not
// visible inside functions; all values inlined below.

fn init_strategy() {
    #{
        name: "Exclusive-order-flow arbitrage",
        version: "3.0.0",
        author: "arbx-cartridge-generator-v3",
        description: "Evaluate only explicitly authorized feed; never infer or scrape unavailable private flow.",
        category: "intents_solver_engine",
        mev_id: "MEV-09-010",
        detector_id: "I_ORDERFLOW",
        execution_class: "AUTHORIZED_FLOW_ONLY",
        min_legs: 2,
        max_legs: 12,
        primary_operators: [25, 27, 24],
        secondary_operators: [11, 22, 23],
        triggers: ["new_block"],
        required_bindings: [],
        target_chains: [],
        min_eval_interval_ms: 250,
        config_schema: #{
            enabled: #{ "type": "bool", "default": true, "editable": true }
        }
    }
}

fn evaluate_opportunity(pool_data) {
    // Fail-honest: no executable settlement / external data binding available.
    #{
        is_opportunity: false,
        estimated_profit: 0.0,
        confidence: 0.0,
        urgency: "monitor",
        reason: "authorized_flow_unavailable",
        detector_id: "I_ORDERFLOW",
        mev_id: "MEV-09-010"
    }
}

fn build_payload(opportunity) {
    #{
        target_contract: "0x0000000000000000000000000000000000000000",
        calldata: "0x",
        value_wei: "0",
        gas_limit: 0,
        max_priority_fee_gwei: 0.0,
        deadline_ts: 0,
        mev_id: "MEV-09-010",
        detector_id: "I_ORDERFLOW"
    }
}
