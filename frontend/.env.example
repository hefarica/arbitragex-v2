# API server (Express + Socket.IO). Used by the frontend for both
# REST (fetch) and WebSocket (socket.io-client) connections.
# Local dev:  http://localhost:3000
# VPS:        https://api.<your-host>   (must allow ws/wss upgrade)
NEXT_PUBLIC_API_URL=http://localhost:3000
