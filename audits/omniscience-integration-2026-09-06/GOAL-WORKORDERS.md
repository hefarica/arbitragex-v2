# /goal — arbx_report.md desglosado en Work-Orders
**Fuente:** `C:\Users\HFRC\AppData\Local\Temp\arbx_report.md` (informe Crypto Deep Analyser 2026-09-06: 7 analistas + 33 verificaciones adversariales, 28 confirmadas)
**Orquestador:** IA OMEGA · **Regla permanente:** edición local permitida, CERO commit/push/PR/deploy (protocolo operador 2026-08-23, transición por PRs explícitos). Cada WO lleva su gate.

## Kanban

| WO | Ítem del informe | Acción | Dueño/Oleada | Estado |
|---|---|---|---|---|
| WO-01 | Broadcast WS insignia emitiendo a nadie (`new_opportunity` server vs `opportunity:detected` client) — N3 crítico #1 | Fix aditivo en `frontend/lib/websocket-client.ts`: escuchar también `new_opportunity` mapeado al mismo handler + test | **Oleada 3** — applier TS | **APPLIED_VERIFIED** ✅ vitest 12/12 + tsc exit 0 + no-regresión 510 tests verdes (pendiente: decisión promote-or-retire de la lib — P0-2 del roadmap) |
| WO-02 | `HotPathEmitter` 0 call-sites, streams hot XLEN=0 perpetuos — N3 crítico #2 | Diseñar wiring exacto en searcher pipeline O eliminación del cable muerto, con invariante XLEN delta | **Oleada 3 (AHORA)** — diseño; aplicar tras cierre auditorías (Rust) | **DISEÑADO** ✅ WO-02-DESIGN.md (cablear `emit_simulated` post-publish, NO `emit_detected`, NO eliminar; 3 defectos latentes corregidos en diff; `cargo check` EXIT=0) |
| WO-03 | Credenciales API en texto plano en Postgres (secret_value TEXT; cifrado pgcrypto/Vault nunca aterrizó; Vault SEALED sin consumidor) — informe §2.5 | Diseño cifrado envelope (pgcrypto) + consumidor Vault opcional + migración SQL + touchpoints `credentials/store.ts` | Oleada 3 — diseño; aplicar tras cierre | **APPLIED_VERIFIED (local)** ✅ WO-03-DESIGN + WO-03-APPLY (gap verificado: la 057 prometía cifrado en "la 058" que es otra tabla; 8 decisiones D1-D8 implementadas textuales: envelope AES-256+MDC por fila, master key SOLO env/file, backfill boot crash-safe 3-fases, sin key = legacy PERO crash on boot con ciphertext RULE 02, `secret_hint` jamás descifra; **colisión-119 ejecutada según ruling orquestador**: aterrizó `120_…sql` con rename + 14 refs + errata en design citando el ruling; **dos agentes un WO**: el caído escribió todo sin verificar → reemplazo auditó línea a línea, corrigió 1 defecto real del test heredado (aserción sync sobre fn async → `await rejects`), +3 tests proyección (descifra-pre-mirror / sin-key ⇒ Redis VACÍO jamás parcial / envelope incompleto ⇒ skip R8); **gates: tsc EXIT 0 · 729/729 vitest api-server (59 archivos) · lint-migration OK**; rotación con `_PREV` + rollback SQL documentados; out-of-claim para PR operador: run_migrations.sh vars + compose env + .env VPS) |
| WO-04 | Literales económicos vivos: tip 2 gwei `bundle_builder.rs:171` · GAS_COST_USD=30 `liquidation_worker.rs:147`+`liquidation_engine.rs:60` · 30bps `computeSimulatedNet.ts:138` — informe §2.5 | Parametrizar (config declarativa validada / lectura on-chain donde aplique) con defaults documentados; diffs exactos | Oleada 3 — diseño; aplicar tras cierre | **½ TS APPLIED_VERIFIED** ✅ WO-04-APPLY-TS.md (D3+D12..D15: knob `lp_fee_default_pct` end-to-end — zod fail-fast bounds 0..0.5, columna PG posición 23 ↔ 33 placeholders alineados, Redis snapshot con default 0.003 pre-migración, `LP_FEE_FRACTION_DEFAULT` ELIMINADO; invariante nota byte-idéntica `lp-fee=30bps-proxy`; tests Caso1 blob pre-migración + Caso2 override 10bps → inverseSize sensibe; 4 gates EXIT 0) · **+§7 RE-VERIFIED (agente que cierra, patrón RESPAWN-2 — verificación jamás heredada)**: diff 123+/8− re-leído completo vs diseño, alineación INSERT contada a mano (33↔33↔33, `$23`↔col 23), gates re-ejecutados (vitest targeted 30 passed + tsc×2 EXIT 0), cero refs colgantes (`LP_FEE_FRACTION_DEFAULT` solo en tombstones; string `lp-fee=30bps-proxy` ÚNICAMENTE en el test que pinnea el contrato), claim frontend re-verificada por lectura (schemas.ts:504 zod no-strict descarta la clave nueva); suite completa EXIT 1 con único failure FUERA de claim (untracked de WO paralelo, desacoplado por grep — declarado) · ½ Rust (D1-D11) en serial-group |
| WO-05 | Código fantasma en ruta de capital: `mod executor` (~350 líneas nunca compiladas) · `NonceManager::refresh` sin call-sites · Eden/Beaver 0 líneas — informe §2.5 | Análisis integrar-vs-eliminar con riesgo; diff propuesto | Oleada 3 — diseño; aplicar tras cierre | **DISEÑADO** ✅ WO-05-DESIGN.md (⚠️ file en disco desde 18:59, agente murió pre-return — recuperado por orquestador; forense completo: (a) executor/ nació muerto — `mod executor;` JAMÁS existió en main.rs, 4 bloqueantes de compilación prueban que nunca compiló, 0 refs → **ELIMINAR** (-350 líneas, historia en git); (b) `refresh` = gap REAL de wiring — **defecto latente LIVE**: cada bundle que consume nonce y NO aterriza deja el contador ADELANTE → todos los bundles posteriores firman nonces demasiado altos → atasco silencioso hasta reboot; 5 puntos de fuga anclados (relays-failed / callBundle-abort / inclusion-timeout / build-error-post-nonce / paper-con-signer) → **INTEGRAR** helper `resync_nonce` fail-soft + 5 call-sites; (c) Eden/Beaver 0 líneas reales (todos falsos positivos `pr-eden-ce`) → OPERADOR onboarding catálogo DB, sin código YAGNI; Opción C recomendada bajo pregunta canónica §34.4; §34.3/live_exec_policy INTOCADOS; gates arbx-pre-edit-audit + risk-limits satisfechos; verificación applier: cargo check+clippy -D warnings+fmt+test -p relays-client) |
| WO-06 | Flood spam XEN+AGLD = 71-78% del flujo (48-58K detecciones/24h, 100% rejected) — informe #4 | Filtro/tiering de watchlist para microcaps spam (token-risk screen); no hardcode de tokens individuales (RULE 00) — criterio dinerámico | Oleada 4 (tras cierre) | **DISEÑADO+VERIFIED** ✅ WO-06-DESIGN + WO-06-VERIFY (math-validator: **PASS-NO-BLOCK** · 30+ anclas file:line verificadas vs HEAD · trampa ruta-level vs leg-level confirmada evitada contra test real · 0 contraejemplos legítimos muertos · RULE 00 auditada (addresses solo en cfg(test)) · **3 MEDIUM al implementar**: M1 spec test T9 aritméticamente MAL (esperado Some(200_200.0) no Some(2200.0) — riesgo: implementador "corrige" el kernel correcto) · M2 I4 salta score_and_publish + counter OPPORTUNITIES_TOTAL{rejected} → dashboards verían −97% sin composición declarada `rejected + flood_suppressed` · M3 traza durable F1 (counter resetea en restart; summary info! declarado sin diff) → sumar a ScannerCounters/heartbeat) |
| WO-07 | Calibración bayesiana inerte: store vacío, flat prior, 0 claves `*calib*` en Redis, 31 operadores sin señal — informe §2.6 | Activar writer de calibración (diseño existente en branch PR#472, κ=20/backoff — NO mergeado) | Oleada 4 | **DISEÑADO+VERIFIED** ✅ WO-07-DESIGN.md + WO-07-VERIFY.md (math-validator: **PASS-NO-BLOCK**, 0 CRITICAL · estadística re-derivada desde el blob — shrinkage Beta-Bernoulli exacto, aritmética re-computada · colisión 111 DROP = única resolución válida (cherry-pick habría aterrizado migración duplicada) · 8 archivos del port = drift CERO vs origin/main · 4 MINOR a corregir antes del apply: cota \|log_lr\| real 2·ln(9999)≈18.4 (no 9.2), predicado tick `AND actual_profit_usd IS NOT NULL`, frase §8 drift, errata blob-hash 94a5ae→ed · charter refutado: cartridge_boot LIMPIO (intersección port∩árbol-sucio = ∅); flip = operador P1-3) |
| WO-08 | Badge socket leyó "LIVE" con subsistemas CONNECTING — informe §5 | Fix honesto: LIVE solo con TODOS los subsistemas conectados (R8) | **Oleada 3** — applier TS | **APPLIED_VERIFIED** ✅ suite 25/25 (18 intactos + 7 nuevos) + tsc exit 0 |
| WO-09 | CSP `unsafe-inline`/`unsafe-eval` y REPORT-ONLY (nada enforcing) — informe §2.5 + ground-truth orquestador | Endurecimiento gradual: directiva enforcing candidata + plan de remoción unsafe (nonce). Aplicar con build + L4 (rompería prod si se aplica a ciegas) | Oleada 3 — diseño; aplicar en ventana de deploy | **APPLIED_VERIFIED (gated-OFF)** ✅ WO-09-APPLY.md: +11/−0 next.config.js; probe 3-vías (gate OFF default / ON byte-identical / `True` fail-closed) + guard 6/6 + tsc EXIT=0; flip = OPERADOR (checklist 6 pasos: PR wiring → ventana quieta P0-5 → `ARBX_CSP_ENFORCE=true` + rebuild RULE 03 → curl ambas cabeceras → smoke e2e → ventana 48-72h con abort/rollback §2.5-2.6); fase 2 (nonce) diseñada íntegra, bloqueo duro documentado (`deploy-pipeline/page.tsx:56` force-static); protección XSS sigue = 0 hasta el flip (honesto R8) · **+§6 re-verificación independiente (gang v1.2 respawn)**: diff re-inspeccionado byte a byte + suite completa re-ejecutada desde cero SIN confiar en el pase original + fail-closed extra `=1` probado (5/5 probes ✅, guard 6/6, tsc 0; 0 requests dominio nuevos, presupuesto 1/2 intacto) — sin cambios necesarios |
| WO-10 | Latencia detección→broadcast NO instrumentada (MN-006) — informe §6.11 | Instrumentar percentiles end-to-end + dashboard | Oleada 4 | **APPLIED_VERIFIED** ✅ WO-10-DESIGN + WO-10-APPLY (respawn halló el apply del caído-429 YA aterrizado en los 4 archivos sin diseño/reporte/verificación — completados por el reemplazo + 1 comentario falso corregido; familia `arbx_pipeline_latency_seconds{stage}` ×6 — `decode_route`/`decode_calldata`/`construction_to_publish`/`emit_boundary`/`publish_xadd`/`decode_to_publish_legacy` — en la REGISTRY del exporter vivo searcher:9001, scrape Prometheus ya configurado prometheus.prod.yml:30-32, p50/p95/p99 vía `histogram_quantile`; hijos `Lazy<Histogram>` pre-resueltos = zero-alloc, R9-silencioso en Rust; guard R8 `arbx_pipeline_latency_invalid_total{stage,reason}` (clock-step/stale contados, jamás inyectados, 2 tests); pierna TS api-server = ring 60s + 1 summary log (NO-MEDIR declarado: skew cross-container, ventanas post-recreate §0); anclas scanner.rs:1505/1547/2044/2243/2288/2338/2643 + opportunity_emitter.rs:298/333/414/439 + publisher.rs:70-151/162-174 + websocket.ts:447-464/958-972; gates: cargo check EXIT=0 0-warn + cargo test publisher 3/3 + vitest wo10 7/7 + familia websocket 17/17 no-regresión + tsc EXIT=0; lazy-init deliberado ⇒ serie ausente = "no computado" honesto hasta PR+deploy operador; follow-ups fuera de claim: histograma shared-ts 5 líneas pierna NOTIFY→WS + panel Grafana detection-pipeline.json) |
| WO-11 | Kill-switch "<10ms" autodeclarado sin confirmación — informe §6.12 | Benchmark reproducible del kill-switch | Oleada 4 | **DISEÑADO** ✅ WO-11-DESIGN.md (veredicto: el claim "<10ms" es INDEFINIBLE end-to-end — mecanismo real = poll (caché TTL 1s + BLOCK 2000ms) ⇒ K5 detección worst-case ~3 s; UI dice "≤ 5 s" y CLAUDE.md §9 dice "<10ms" — se contradicen; vector "File" NO EXISTE en código; 6 segmentos K1-K6 con reloj monotónico + harness A Rust in-process (Redis real DB15) + harness B runbook VPS operator-only; regla: docs se reescriben con el número MEDIDO; ⚠️ 6 divergencias fail-honest declaradas, la más grave: **gate INVERTIDO en drift_tracker** — `if !enabled { continue }` idlea con kill OFF y CORRE con kill ARMADO, contrario a su comentario `drift_tracker.rs:161-167`) + WO-11-APPLY.md (fail-honest §1: el design-agent original cayó → reemplazo autoró el diseño desde cero conforme al charter read-only; harness A/B PROPUESTO no aterrizado; remediables backlog: suscripción Rust KILLSWITCH_CHANNEL baja K5 ~3s→RTT, gate invertido drift_tracker, semántica clave-ausente unificada) · **+apply-pass v2**: fuentes ÍNTEGRAS de ambos harness ahora viven en el design (§3.1 `killswitch_latency.rs` completo — cerró el gap "diffs prometidos vs sketch"; §3.2 `killswitch_bench.sh` completo con aborts de seguridad, containers resueltos no hardcodeados, emergency-disarm en toda salida, R9 ventana de logs; §3.3 criterios de validez estadística) — listas para aterrizar verbatim por apply-WO u operador · **+§9 LEDGER verificación independiente (51 checks, todas las anclas re-leídas)**: 3 defectos del v1 corregidos — C1 stream inexistente `arbx:opps:scored` citado 2× (RULE 00), C2 "sin Redis mide K1" imposible (connect marca conexión → todos SKIP no_computed), C4 sketches→fuentes íntegras REVIEWED-NOT-COMPILED; endurecimientos C3 (preflight aborta con clave ausente = halt fail-closed vivo A5-STALL, trap-EXIT disarm, `--since` obligatorio); veredicto §0 INALTERADO ("<10ms" indefinible con arquitectura poll viva) |
| WO-12 | REVM 3.5 pinneado (línea vieja); producción corre Anvil fork, no REVM in-process — informe §2.6 | Decisión documentada: upgrade ruta vs Anvil-fork como backend declarado (eliminar claim de fidelidad REVM) | Oleada 4 | **DISEÑADO** ✅ WO-12-DESIGN.md (claim "REVM 3.5" CORREGIDO: pin real revm 42.0.1 — Cargo.toml:35 + Cargo.lock:6194 + /capabilities vivo; la "línea vieja" es un COMENTARIO stale simulator-v2/Cargo.toml:10; decisión = camino (a) REVM in-process declarado Tier 1/2 + Anvil retenido Tier 3 con imagen anclada; runtime verificado SIM_BACKEND=anvil explícito + REVM_RPC_URL ausente; flip = operador §34.3) |
| WO-13 | Edge de producción = Dockerfile.node (dev-local), worker Hono es solo POC (B-02) — informe §2.6 | Cerrar brecha o documentar la decisión arquitectónica | Oleada 4 | **CERRADO** ✅ WO-13-DESIGN+APPLY: la premisa era STALE — B-02 resuelto 2026-08-11 (PR #321 `60f3f702`); prod corre worker Hono canónico vía `node-server.ts` (3 fingerprints VPS: CMD + boot-log único + label compose.prod); raíz del error = memory pre-swap + header "POC" auto-denominado + sufijo `.node` leído como dev-local; apply = 3 headers solo-comentario (typecheck EXIT=0); residuales R-1/R-2 paridad dev→PR aparte, R-3→WO-14, R-4 wrangler sentinels documentados · **+RE-VERIFICACIÓN RESPAWN-2 A (mitad 1)**: evidencia 100% propia — triple fingerprint re-confirmado con inspect fresco (CMD textual == Dockerfile.node:30, label config_files==compose.prod.yml, boot-log único repo-hit node-server.ts:48), STARTED idéntico 23:45:32Z + Up 3h ⇒ ninguna recreación revirtió el swap; `poc/` NO existe (Glob vacío); nota honesta: corrimiento 40→48 = header WO-13 del apply; 0 requests dominio, 1 SSH read-only, VPS no mutado — **cero contradicciones, veredicto BRECHA CERRADA reproducido** |
| WO-14 | 401 webhook alertmanager cada 5 min (N3 §5) + token cloudflared en `systemctl cat` + ALLOWED_ORIGINS vacío | Config VPS — ENTREGAR runbook al operador (agentes NO tocamos VPS: §32/§33) | Runbook operador | **ENTREGADO** ✅ WO-14-RUNBOOK.md (23 KB, ejecutable por el operador: FASE 0 sin downtime + PR-1 D-4 + PR-2 CORS #539 + verificación §6) + WO-14-DESIGN.md companion (adjudica autocontención: 4 diffs canónicos verbatim ✔, 12/12 citas vs código ✔, COMPLETA los 3 sub-diffs gemelos-dev que el runbook solo referenciaba; fix clave = `http_config.headers` NO Bearer — el gate solo lee `x-arbx-admin-token`; render step sed/dunder sin rebuild de imagen; cero secretos, placeholders only) · **+§9 pase de verificación RESPAWN-2 B (adversarial sobre la mitad segunda)**: cazó 5 defectos V-1..V-5 del pase A — V-2 grave: atribución INVERTIDA del wildcard (edge worker SÍ soporta `*` con credenciales suprimidas index.ts:290-299; quien rechaza `*` es el WS api-server websocket.ts:245-247) — corregida; V-3: drift +4 en TODAS las citas compose.prod.yml>L406 (lo desplazó el comentario del apply WO-13) — corregidas en 6 sitios, contenido de diffs intacto; V-1 cita stale :146-149→:245-251; V-5 claim "12/12 exactas" falsado→10/12 honesto; refs runbook↔companion resuelven sin renumeración; no-verificado declarado R8 (hechos VPS §0 heredados @00:21Z; storage.path flag de imagen no verificable sin pull) |
| WO-15 | 60 consumers huérfanos `ws-emitter-g0` sin DELCONSUMER (N3) | `XGROUP DELCONSUMER`/`XAUTOCLAIM` en shutdown del api-server | Oleada 4 | **DISEÑADO + APPLIED (local) + VERIFIED (respawn A) + RUNBOOK** ✅ WO-15-DESIGN.md + WO-15-APPLY.md (censo VPS real: **356 consumers / 7 grupos, ~351 huérfanos** ~200h — no 60; gotcha: container = `arbitragex-v2-redis-1`, NO `redis`; fuga = consumerName único por boot + jamás DELCONSUMER ni XACK; aplicado en `websocket.ts` +330 líneas: stop() drena XREADGROUP en vuelo ≤3s ANTES de DELCONSUMER×2 + maintenanceLoop purge idle>30min orden pérdida-cero XPENDING→XAUTOCLAIM→XACK→DELCONSUMER + XACK batch post-emit; **re-verificación RESPAWN-2 A sin confiar en el caído**: forense del árbol (agente original murió post-código pre-reporte), tsc exit 0 + 5 archivos websocket 17/17 tests (incl. 3 nuevos + `websocket-wo10.test.ts` de WO-10), censo fresco sin deriva (356/~351), XLEN=0 en hot:* ⇒ limpieza hoy riesgo-cero, anomalía sim-ctl pending 1→0 declarada fail-honest sin interpretar; **hallazgo D-10**: lag 10,548 > XLEN 10,001 ⇒ 483-547 entradas trimmeadas SIN consumir (hoy inerte, crítico post-flip) — decisión operador; runbook FASE A/B + invariante XLEN; follow-ups: patrón replicable en 4 superficies + relays-client-g0 ldi 0-0) |

## NO resoluble por agentes (decisiones del operador / gated)
- **Token propio**: el informe verifica N/A estructural (el repo no acuña; "ARBX" colisiona con ≥3 proyectos ajenos). Crear token = decisión de negocio del operador, NO del equipo.
- **Auditoría externa** (Trail of Bits/OpenZeppelin/Spearbit): contratación del operador. El equipo puede preparar el paquete de readiness.
- **Flip a LIVE_MAINNET**: §34.3 — default-deny + `MainnetRefused` se MANTIENEN intactos (el propio informe destaca la barrera fail-closed como la mayor fortaleza real). Flip = operador con autorización explícita tras gates PASS.
- **Cadena admin/contratos deploy**: direcciones TBD — runbook del operador.
- **Mercado hostil** (margen sub-dólar/evento, top-1% captura 80%, 90%+ flujo privado): no es un bug — se ataca con WO-10 (latencia) + relays privados + WO-07 (calibración), no con optimismo.

## Cronología de oleadas
1. **Oleada 1** (corriendo): auditoría estática 0-100% — 20 agentes.
2. **Oleada 2** (corriendo): round-table integración 4 capas — 17 agentes.
3. **Oleada 3** COMPLETADA (wf_1d53ea93 · 5/7 vivos · 703K tokens): WO-01+WO-08 **APPLIED_VERIFIED** (vitest 12/12 y 25/25, tsc exit 0, 510 tests no-regresión) · WO-02+WO-04+WO-05 **DESIGNED** · muertos por 429: validator TS peer-review + design WO-03.
4. **Oleada 4 = GANG OMNISCIENCE §9** (workflow `omniscience-gang`): re-despacho de los 2 gaps-429 + appliers Rust WO-02/04/05 (serie total, `target/` caliente §36.4) + WO-06/07/10..15 + **browser-verifiers que dan fe como humanos en la DApp viva** + loop éxito-o-éxito.
5. **⛔ MURO 429 SISTÉMICO (2026-09-06/07, wf_29b60a15-af2)**: 71 agentes despachados · 8 completos · **63 muertos por `429 All target providers failed`** — fases Cross (24+ verificadores) y Browse (3 testigos) aniquiladas; applies WO-02-Rust/WO-04-Rust/WO-05/WO-10 muertos con todos sus respawns. Dominio público LIMPIO (edge: 0×429 en 3h — NO es el dominio, es el proveedor LLM). Causa raíz: burst de 16 concurrentes + RESPAWN-2 amplificando (cada muerte engendraba 2). **Circuit-breaker v1.2** instalado por orden del operador (oleadas de 4 + presupuesto global de respawns 8 + tripwire sistémico + honest_final DEGRADADO honesto) — relanzamiento con `resumeFromRunId` (cache preserva los 8 completos). El `honest_final` "ÉXITO" del run degradado fue un overclaim — corregido R8.
