# Plan 5W2H — LIVE-READINESS 100% verde (2026-09-22)

> /goal (operador): https://arbx.ape-tv.net/live-readiness sin alertas, sin advertencias,
> todos los checks superados. Extiende el plan canónico
> `docs/superpowers/plans/2026-09-21-live-readiness-5w2h.md` (NO lo duplica: hereda FASES 1-4).
> BOARD: `GOAL-WORKORDERS.md` (este directorio) + umbrella `audits/live-readiness-20260921/`.

## Estado de la página al abrir (16:39Z, post-recovery)
- System guard verde (PG/Redis/HB/Engine/A.4/A.5). Readiness 16/19: 2 parciales (G-PAP-1, G-DISK-1), 1 failing (G-SIM-1, 5 evidencias stale).
- Consola: errores CORS edge-arbx en opportunities/live + quote/anchor cada ~30s.
- Stepper 0/4 (`topology_vault_empty`) — REGRESIÓN vs 09-21 (era 4/4 PASS, WO-LR0.1).
- Scanner pipeline IDLE total (ver WO-LR22.1) — bloqueante upstream de casi todo.

## Qué se arregló HOY (orquestador, con evidencia)
| Fix | Evidencia |
|---|---|
| FE-EDGE-DIRECT-01 (CORS ACAO '') | `ALLOWED_ORIGINS` unset en contenedor edge prod → ACAO vacío. Añadido a `/opt/arbitragex-v2/.env` + `up -d edge` (R3, compose.prod.yml). Verificado: HTTP/2 200 + ACAO correcto en edge-arbx. Consola: errores opportunities/live ELIMINADOS. |
| ROLLUP-GAP-01 (p20260920) | 11 buckets `__totals__` zero-fill honestos → coverage gate PASS → p20260921 dropeable por cron 04:17Z. Fix durable del script = WO-LR22.8. |
| Recovery cadena disco-lleno→PG→Redis AOF | (sesión previa, ver resumen) PG up 6h, Redis/api-server/edge up y healthy. |

---

## WO-LR22.1 — SCANNER-SILENT-02 (P0, bloqueante upstream)
- **What**: reactivar el pipeline de detección (pending→decode→gates→emit) sin mocks (RULE 00/R8).
- **Why**: `arbx:opps:detected` congelado en XLEN=10003, última detección 10:19:00Z. Heartbeat
  con todos los contadores a 0: pending_received=0, decoded=0, *_scanned=0, passed_all_gates=0.
  Es la causa raíz de: quote_anchor 503 (sin publish), Sims Today 0, G-PAP-1 sin reloj, Paper
  Shadow INACTIVE, 3 breakers NOT_AVAILABLE.
- **Who**: gang Hermes (agente Rust/pipeline) + validación orquestador.
- **Where**: `backend/searcher-rs/src/scanner.rs` (run_chain, run_subscription), workers,
  VPS `arbitragex-v2-searcher-rs-1`.
- **When**: P0 inmediato — todo lo demás depende.
- **How**: (1) reproducir por qué la tarea de detección murió en silencio (contenedor Up 35h
  SIN reinicio, sin panic en ventana retenida — la muerte es previa a 14:26Z o el task está
  colgado); (2) auditar por qué el watchdog SCANNER-STALL-01 (#624, 60s) NO disparó con 6h de
  stream congelado — GAP del guardián; (3) identificar contribuyentes: tormenta DNS Docker
  (401 fallos "Temporary failure in name resolution" 14-16Z, reconnects Redis), Redis
  BusyLoadingError 14:26Z (restart), backoffs permanentes price (alchemy key 429 crónico;
  coingecko 400+429); (4) fix en raíz + PR durable (no-git-until-final-gate); (5) verificación
  E2E: XLEN delta > 0, heartbeat con pending_received > 0, quote anchor publicado.
- **How much**: 1 agente dedicado + validator; riesgo de regresión controlado por G1-G6.

## WO-LR22.2 — quote_anchor 503 sin ACAO (edge)
- **What**: 2 capas: (a) raíz = scanner idle (hereda LR22.1); (b) defecto edge:
  `proxyPassThrough` NO adjunta headers CORS en respuestas de error → el 503 honesto
  `quote_anchor_not_published` se reporta en el navegador como error CORS (mascara la verdad).
- **Why**: honestidad de observabilidad (R8): un 503 honesto debe verse como 503, no como CORS.
- **Who**: gang (edge/TS) — PR durable [OPERADOR merge].
- **Where**: `edge/worker/src/index.ts` (~:1368 y proxyPassThrough).
- **When**: tras LR22.1 (prioridad media).
- **How**: adjuntar ACAO/ACAC a TODAS las respuestas del proxy (éxito Y error) cuando el
  origin está allowlisted; test contract; sin cambios de semántica de cache.
- **How much**: PR pequeño + contract test.

## WO-LR22.3 — Stepper 0/4 topology_vault_empty (REGRESIÓN)
- **What**: `/api/readiness/steps` Paso 1 FALLA: "no non-empty snapshot persisted. Publish ≥1
  RPC + ≥1 WSS provider in the Topology Vault" — el 09-21 estaba 4/4 PASS.
- **Why**: el snapshot del vault probablemente se perdió en la cadena de crash
  (Redis wipe/AOF repair o PG panic) o su publicador no ha vuelto a emitir.
- **Who**: gang (readiness/api-server).
- **Where**: productor del vault snapshot (api-server/readiness path — localizar).
- **When**: paralelo a LR22.1.
- **How**: encontrar el productor real del snapshot, por qué no re-publica tras restart,
  re-publicar con datos reales de config (RPC_WS_* existen en .env — sin inventar nada);
  fix durable del re-publish tras boot si aplica.
- **How much**: investigación acotada + posible PR.

## WO-LR22.4 — G-SIM-1 evidencias stale (5)
- **What**: regenerar evidencias REALES para modules_merged, fork_suite, variance_benchmark,
  eth_callbundle_staging. `second_signoff` queda [OPERADOR] (G-SIM-1 no puede cerrar solo).
- **Why**: único gate FAILING.
- **Who**: gang (simulación/forense) — ver BOARD forensic-engine-2026-09-21.
- **When**: tras scanner activo (fork_suite/variance necesitan tráfico real).
- **How**: artefactos reproducibles (§34.5.3: sólo artefactos, jamás afirmaciones).
- **How much**: alto (depende de pipeline vivo).

## WO-LR22.5 — G-PAP-1 reloj 7 días
- **What**: el reloj no arranca hasta que haya candidatos que crucen todos los gates.
- **Why**: hereda LR22.1 (0 sims persistidas = 0 viables).
- **How**: sin acción directa — verificación diaria de que el reloj avance una vez LR22.1 cerrado.

## WO-LR22.6 — G-DISK-1 87.9% (18.1GB libres)
- **What**: verificación del cron pg_retention.sh 04:17Z (dropea p20260921 ~31GB; coverage
  gate YA pasa tras ROLLUP-GAP-01 fix de hoy).
- **When**: mañana 09-23 tras 04:17Z.
- **How**: `df -h /` + blockers endpoint SIN g_disk_1. Si el cron NO dropea → RC en el script
  (auditar output del cron, no asumir).

## WO-LR22.7 — ROLLUP-GAP-01 fix durable
- **What**: parchear `pg_retention.sh` (o el rollup job) para materializar buckets zero-fill
  cuando el día no tiene outcomes en horas iniciales — hoy se hizo manual (SQL honesto, mismo
  valor semántico que los zero-rows existentes).
- **Who**: gang (DB) — PR [OPERADOR merge].
- **How**: INSERT ... SELECT generate_series ... WHERE NOT EXISTS (idéntico al aplicado),
  en el paso previo al coverage gate. Test con día de 0 filas.

## WO-LR22.8 — Tormenta DNS Docker (401 fallos/3h)
- **What**: investigar "Temporary failure in name resolution" desde searcher-rs durante/tras
  restarts de Redis (BusyLoading 14:26Z). ¿Embedded DNS saturado por reconnect storm? ¿Timeout
  de resolución en el cliente redis?
- **Why**: cada reconnect con fallo DNS = ventana de pérdida de stream/tick; agrava LR22.1.
- **How**: correlacionar timeline DNS-fails vs Redis restarts; si es reconnect-hammer, considerar
  backoff jittered en ConnectionManager (PR) o `dns:` explícito en compose (investigar, no
  adivinar).

## WO-LR22.9 — [OPERADOR] No autónomos (documentados)
- A.9 quorum-2 sign-off (ledger frozen 2026-09-07, hash 5189814f…, 0 sign-offs).
- G-SIM-1 `second_signoff`.
- Merges de PRs (no-git-until-final-gate). Flips §34 (intactos).

## Orden de ejecución del gang
LR22.1 (P0) → {LR22.2, LR22.3, LR22.8 en paralelo} → LR22.4 → verificación LR22.6 (mañana).
