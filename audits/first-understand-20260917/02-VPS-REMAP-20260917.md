# Re-mapa VPS read-only — 2026-09-17 (orquestador, ssh arbx)

> Reverificación de los hallazgos del mapa 2026-09-16 (audits/live-eng-map-20260916/).
> Todo read-only. VPS repo: `a06a968d` (main) — los 3 commits locales (fdb40401, 125b1e0b,
> dcfe890c + diff route_intent.rs) NO están desplegados.

## Hallazgos persistentes (NO remediados desde 2026-09-16)

| # | Hallazgo | Estado 09-16 | Estado 09-17 (hoy) | Evidencia |
|---|---|---|---|---|
| 1 | disk_guard roto | script 0644 → Permission denied | **ROTO IGUAL** — `-rw-r--r--` root:root, cron entry existe (`13 * * * *`) pero el script no es ejecutable | `ls -la scripts/disk_guard.sh` + `crontab -l` |
| 2 | .env world-readable | 0644 + 5 baks | **IGUAL** — `.env` 644 root, baks presentes (.bak.20260819-gprice1, -a4token-20260820, -anvil-fork-20260829, …) | `stat -c "%a"` |
| 3 | Build cache | 21GB reclaimable | **21.24GB reclaimable** (181 entradas, 0 activas) | `docker system df` |

## Hallazgos NUEVOS hoy

| # | Hallazgo | Detalle | Riesgo |
|---|---|---|---|
| 4 | `/tmp/arbx-deploy.lock` | creado 05:49 UTC hoy; **VERIFICADO STALE**: `ps aux` no muestra proceso deploy activo (solo `tail -f` huérfanos de logs deploy338-341 de Aug 15/16 + thanos). Flota reiniciada ~03:46 UTC hoy por causa aún no determinada | Deploy NO en curso. El lock debe limpiarse antes del próximo deploy (acción VPS, gated) |

## Confirmación dura del hallazgo 1 (disk_guard)

`/var/log/arbx-disk-guard.log` emite `Permission denied` cada hora (cron dispara, script 644 no ejecuta):
el guard del PR #557 sigue **NO operativo** — sin alarma de disco posible.
| 5 | Disco | 104G/150G usado (73%), 41G libres | OK con margen (>30GB floor del guard), pero guard inoperativo (hallazgo 1) = sin alarma si revienta |

## Flota (evidencia de salud)

24 contenedores, TODOS `(healthy)`. Thanos trio "Up 4 weeks", resto "Up 2 hours"
(reinicio de flota ~03:46 UTC de hoy, causa no determinada desde lectura — mismo SHA a06a968d).

## Bloqueos para avanzar de estado (§4 live-engineering)

- VPS_READ_ONLY → VPS_MAPPED requiere: causa del reinicio de flota + dueño del lock 05:49.
- Remediaciones 1-3 son cambios de VPS (chmod, cron, prune) = CHANGE_AUTHORIZED, gated operador.

## Pendiente de esta oleada

- WO-05 `cargo check --workspace` (corriendo local, background).
- WO-02 gang fichas hot-path (corriendo, wf_7ef284a7-b74).
