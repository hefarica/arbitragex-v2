# WO-02a-FIX-G1 (2026-09-17) — Cierre de G1 del cross-exam + precisión del mecanismo del piso USD

> **Reporte completado por REEMPLAZO A del gang (respawn ronda 1).** El fixer original
> aterrizó los adenda en ambos archivos de claim pero murió ANTES de escribir este
> reporte (referenciado en WO-02a-verify-VERIFY.md:565 pero inexistente en disco al
> llegar el reemplazo — verificado `ls` 2026-09-17). Este documento: (a) registra la
> autoría dividida honestamente, (b) anexa la RE-VERIFICACIÓN independiente byte-exact
> de TODAS las citas del fix caído (precedente "aserciones de agentes ≠ facts"), y
> (c) documenta una imprecisión residual de cita encontrada por el reemplazo.
> Reglas duras: 0 commit/push/PR/deploy, 0 cargo/build (board WO-02 los prohíbe),
> 0 VPS, 0 HTTP, 0 mocks. Solo Read/Grep/Bash-readonly + git read-only como evidencia.

## 1. Charter (G1 de la TABLA del cross-exam, WO-02a-verify-CROSS-EXAM.md §4-G1)

Corregir la conclusión "el sistema opera con umbrales RELATIVOS, no con un blanco USD
absoluto" (WO-02a-DESIGN.md §9:302-303; consumida por WO-02a-verify-VERIFY.md §3):
reconocer el piso USD absoluto operador-configurable en el hot-path del scanner y los
blancos USD canónicos de shared-rs, con adendum append-only en WO-02a-DESIGN.md y
WO-02a-verify-VERIFY.md citando scanner.rs:2568 y trading_config.rs:166/249/260/622.
NO tocar el GAP por-estrategia (sigue válido).

## 2. Estado al llegar el reemplazo — G1 YA ATERRIZADO por pares concurrentes

Respawn-2 en acción: el trabajo sustantivo estaba hecho por TRES fixers paralelos
(ningún reporte del tercero sobrevivió):

1. **DESIGN §9.1 ADENDUM (WO-02a-FIX-R1)** (`WO-02a-DESIGN.md`:305-349) — re-stringe
   el §9: umbrales relativos vivos (min_net_bps, GasFloorBreach) + piso USD absoluto
   `trading_config.min_profit_usd` + 4 knobs USD workbook declared-only +
   reconciliación con 02d:212/§5:245-258 y 02b:148.
2. **VERIFY §10 ERRATA G2** (`WO-02a-verify-VERIFY.md`:297-404) — tabla de
   reconciliación canónica 10.3: GAP por-estrategia COEXISTE con blancos USD runtime
   globales (dominios distintos).
3. **`// WO-02a-FIX-G1`** (VERIFY:486-573) + **espejo DESIGN §9.2** (:364-396, marker
   `// WO-02a (2026-09-17)`) — precisión material: scanner.rs:2568 puebla
   `min_profit_threshold` pero `score()` jamás lo lee; el piso VIVO gatea por
   `config_aware.rs → risk_engine.rs` y el override por estrategia por
   `strategy_config_gate.rs → scanner.rs:2312`.

Este reemplazo NO duplicó ninguna sección. Verificó todo (§3) y completó los
entregables huérfanos: ESTE reporte + entrada del board (GOAL-WORKORDERS.md).

## 3. RE-VERIFICACIÓN independiente del reemplazo (byte-exact, 2026-09-17)

Todas las citas del fix caído re-abiertas contra disco ANTES de escribir:

| Cita | Veredicto | Contenido verificado |
|---|---|---|
| scanner.rs:2568 | EXACTO | `min_profit_threshold: cfg.min_profit_usd` dentro de `PrioritizationEngine` (rama passed_all_gates :2562-2569) |
| scanner.rs:1488 | EXACTO | `async fn decode_and_score_tx` — sin feature-gate (fn plano, default build) |
| scanner.rs:1637 | EXACTO | `cfg_opt = trading_config.state(chain_id)` — Redis-backed, superficie operador |
| scanner.rs:2024 | EXACTO | `let Some(cfg) = cfg_opt else` → observe-only R8 sin config |
| scanner.rs:2125 | EXACTO | `ConfigAwareEvaluator::with_cache(&cfg, …)` |
| scanner.rs:2310-2319 | EXACTO | gate bloqueado → `rejection_reason = Some(format!("{tag}:{reason:?}"))` (fail-honest R8) |
| prioritization-spine/src/scoring.rs:13-14 | EXACTO | `PrioritizationEngine { pub min_profit_threshold: f64 }` |
| scoring.rs:23-27 | EXACTO | ÚNICO gate de `score()` = `net_expected <= 0.0 → NegativeProfit`; archivo 47 líneas, `min_profit_threshold` 0 lecturas |
| grep `min_profit_threshold` backend/*.rs | EXACTO | exactamente 2 hits: scoring.rs:14 (decl) + scanner.rs:2568 (población). Cero lectores |
| prioritization-spine/src/config_aware.rs:148-151 | EXACTO | `policy_from_config`: `min_net_profit_usd: cfg.min_profit_usd` |
| config_aware.rs:1053 | EXACTO | invocación `policy_from_config(self.config, effective_capital)` |
| config_aware.rs:1085 | EXACTO | `validate_opportunity_risk(&risk_profile, &policy)` |
| math-engine/src/risk_engine.rs:47-48 | EXACTO | `net_profit_usd <= policy.min_net_profit_usd → Err(NegativeNetProfit)` |
| strategy_config_gate.rs:302-308 | EXACTO | `strat_cfg.min_profit_usd` → `Reject(StrategyConfigBelowMinProfit { threshold_usd })` |
| trading_config.rs:156 | EXACTO | per-strategy `min_profit_usd: Option<f64>` (en StrategyConfig) |
| trading_config.rs:166 | EXACTO | `simulation_capital_usd: Option<f64>` (per-strategy) |
| trading_config.rs:249 | EXACTO | `simulation_target_profit_usd` + doc :241-248 "backend STORES but does NOT gate (R8)" |
| trading_config.rs:260 | EXACTO | chain-level `min_profit_usd: f64` (gate real) |
| trading_config.rs:622-626 | EXACTO | `effective_min_profit_usd(strategy)` helper — ver §4 abajo |
| engines/mod.rs:25-28 / :38-52 | EXACTO | 4 estables ungated + 7 gated (miscount "8" ya corregido por FIX-G3) |

**Consumidores TS re-verificados** (blancos canónicos stores-not-gates):
`simulation_target_profit_usd` consumido por api-server TS
(api-server/src/simulation/computeSimulatedNet.ts:359, snapshot
tradingConfigSnapshot.ts:190) — capa UI/simulación, NO gate del searcher.

## 4. IMPRECISIÓN RESIDUAL encontrada por el reemplazo (menor, documental)

VERIFY:544-545 y DESIGN §9.2 dicen que `effective_min_profit_usd` (trading_config.rs:622)
es "consumido por strategy_config_gate.rs:302". **Impreciso en el cable, correcto en la
semántica**: grep `effective_min_profit_usd(` en backend/ (--include=*.rs, sin target/)
= SOLO trading_config.rs (definición :622 + tests :1063/:1077/:1079) — **0 call-sites de
producción del helper**. El override por estrategia se aplica en
strategy_config_gate.rs:302-304 leyendo DIRECTAMENTE `strat_cfg.min_profit_usd`
(el campo per-strategy Option, trading_config.rs:156), no vía el helper :622 (que
implementa la misma fallback-chain chain-level como función de conveniencia).
La conclusión NO cambia (el override per-estrategia ES consumido en :302); solo la
atribución del cable. Quien cite el override debe citar
`trading_config.rs:156 → strategy_config_gate.rs:302`, reservando :622 como helper
sin consumidores productivos (mismo patrón declared-only ya catalogado).

## 5. Conclusión canónica vigente tras G1 (para gates 1-8 / WO-06)

- El board NO debe consumir "umbrales solo relativos". Lectura correcta: **umbrales
  relativos vivos (min_net_bps ranking, GasFloorBreach) COEXISTEN con un piso USD
  absoluto operador-configurable VIVO en el hot-path** del scanner, cuyo cable de gate
  es `config_aware.rs:150 → risk_engine.rs:47-48` (+ override per-estrategia
  `trading_config.rs:156 → strategy_config_gate.rs:302 → scanner.rs:2310-2319`), NO
  scanner.rs:2568 (población de campo muerto en `score()`).
- Blancos USD canónicos del operador existen en shared-rs trading_config:
  `simulation_target_profit_usd` (:249, stores-not-gates R8, UI filter),
  `simulation_capital_usd` (:166/:215), `min_profit_usd` (:156 per-strategy, :260
  chain-level), `capital_usd` (:183, cap 2% en terminus — 02d §5).
- **GAP objetivo_usd POR ESTRATEGIA en searcher-rs: INTACTO — SOBREVIVE** (re-confirmado
  por este reemplazo: ningún `objetivo_usd`/`target_usd` productivo en searcher-rs;
  el patrón grep `target_usd` del verify original no capturaba `target_profit_usd`).
- Clasificación de evidencia: CANONICAL_REPO (todas las citas file:line recomputables).

## 6. Verificación del fix (ejecutada por el reemplazo)

- Marcadores: `grep -c "WO-02a-FIX-G1"` → WO-02a-verify-VERIFY.md = 3 (sección :486 +
  2 referencias internas), WO-02a-DESIGN.md = 2 (§9.2 título + referencia espejo).
- Append-only respetado por el fix caído: §0-§10, FIX-G3 y secciones previas de ambos
  archivos sin ediciones in-place (diff visual de encabezados — sin colisiones).
- ESTE reporte ahora existe → la referencia VERIFY:565 ("Reporte del fixer:
  WO-02a-FIX-G1-20260917.md") deja de ser dangling.
- Board actualizado con entrada `// WO-02a-FIX-G1` en GOAL-WORKORDERS.md (append).
- Reglas duras: 0 git-mutación (solo `git show`/`git log`/`git status` read-only usados
  por el fix caído; este reemplazo usó find/grep/sed/wc), 0 cargo/npm/build (restricción
  board WO-02 cumplida), 0 VPS, 0 HTTP (presupuesto público 0/5 gastado), 0 mocks (RULE 00).

## 7. Claims de archivo de otros WO

- `GOAL-WORKORDERS.md` (board, WO-02 sección): editado SOLO en modo append con la
  entrada FIX-G1 — misma práctica que FIX-G2/G3/ERRATA-2 previas de otros fixers.
- `WO-02a-DESIGN.md` / `WO-02a-verify-VERIFY.md`: NO editados por este reemplazo
  (contenido del fix caído verificado y respetado tal cual; la imprecisión residual §4
  queda documentada AQUÍ, sin tocar el texto del par — regla anti-pisado de claims).
