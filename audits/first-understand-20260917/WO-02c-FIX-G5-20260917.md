# WO-02c-FIX-G5 — Reclasificación "default SIM_BACKEND=anvil" (compose ≠ runtime)

> Fixer gang ronda 1 · 2026-09-17. Cierra **G5** de `WO-02c-CROSS-EXAM.md:131-139`
> (fila :174: "Reclasificar 'default anvil' como INFERRED/UNKNOWN para runtime VPS
> (env_file ../.env puede override) → nota de clasificación en §7/ficha 4.6").
> Veredicto del cross-examiner: VERIFICACIÓN SUSTANTIVA REAL, pero este claim
> estaba sobre-clasificado. Verificación propia del fixer: **CONFIRMADO** (abajo).

## 1. El gap

El entregable `02c-SIM-STACK-SELECTOR.md` ficha 4.6 (:142) y `WO-02c-DESIGN.md`
hallazgo 7 (:22) afirmaban: "default SIM_BACKEND=anvil en compose — compose.prod.yml
NO define SIM_BACKEND → default anvil", presentándolo como verdad RUNTIME. Es verdad
solo del plano COMPOSE: el mismo bloque monta `env_file: ../.env`, y si el .env del
VPS define `SIM_BACKEND=revm`, el default se sobreescribe y cambia TODO el análisis
de cuál branch corre (incluido B2c carrier-first y el scope de G3).

## 2. Evidencia re-verificada por este fixer (no heredada, RULE 00)

1. `docker/compose.prod.yml:145-161` — bloque `sim-ctl:`. `environment:` (:152-157)
   pasa SOLO ARBX_CONFIG_PATH / SIM_PORT / DATABASE_URL / REDIS_URL / ANVIL_URL.
   grep `SIM_BACKEND` en compose.prod.yml = **0 hits** (confirma
   WO-02c-verify-CROSS-EXAM.md:19). Rango exacto del bloque: `sim-ctl:` en :145.
2. `docker/compose.prod.yml:150-151` — `env_file: - ../.env` declarado ANTES de
   `environment` en el MISMO bloque: cualquier `SIM_BACKEND` del .env entra al
   contenedor.
3. `backend/sim-ctl/src/main.rs:661-664` — el default "anvil" vive en el BINARIO:
   `std::env::var("SIM_BACKEND").as_deref().unwrap_or("anvil")`. `:734`: el simulador
   B2c solo se popula si `== "revm"`; `:400`: 501/503 fail-honest si no.
4. `02-VPS-REMAP-20260917.md:12` — la única evidencia VPS sobre el .env es
   `stat -c "%a"` (permisos 644 + 5 baks). NO leyó el contenido → valor runtime
   efectivo = **UNKNOWN desde evidencia local**. Fail-honest R8: se declara, no se
   infiere "anvil" por default.

## 3. Fix aplicado (todas las marcas `// WO-02c-FIX-G5 (2026-09-17)`)

| archivo | lugar | cambio |
|---|---|---|
| `02c-SIM-STACK-SELECTOR.md` | ficha 4.6 (:142) | nota: CANONICAL_REPO SOLO compose; runtime VPS UNKNOWN vía env_file :150-151; main.rs:661-664; 02-VPS-REMAP solo stat (:12) |
| `02c-SIM-STACK-SELECTOR.md` | matriz §8 (:221) | fila tx_builder+sim_engine: "supeditado a SIM_BACKEND vigente — compose no lo define (CANONICAL_REPO), runtime VPS UNKNOWN" |
| `02c-SIM-STACK-SELECTOR.md` | §13 ERRATA (append-only, nuevo) | evidencia completa + cambios + impacto |
| `WO-02c-DESIGN.md` | hallazgo 7 (:22) | sello CANONICAL_REPO ACOTADO a compose; runtime = INFERRED/UNKNOWN |

Clasificación final: **compose = CANONICAL_REPO · runtime VPS = INFERRED/UNKNOWN**.

## 4. Impacto (por qué es load-bearing, no cosmética)

- Si el .env del VPS dijera `SIM_BACKEND=revm`: B2c carrier-first activo en runtime,
  RevmBackend en el path stream, y G3 del cross-exam (dedup parcial
  `WHERE simulator='revm'`, migrations/113:27-29) pasa a ser el camino CALIENTE en
  vez del legacy.
- El mecanismo de selección NO es incierto: el VPS corre a06a968d (02-VPS-REMAP) que
  ya contiene la lectura SIM_BACKEND. La incertidumbre es puramente del VALOR del .env.
- Acción read-only propuesta para WO-06/operador (NO ejecutada — presupuesto HTTP/ssh
  del fixer en 0):
  `ssh arbx grep -c '^SIM_BACKEND=' /opt/arbitragex-v2/.env` (0 = default anvil
  vigente runtime; 1 = leer el valor).

## 5. Sincronía de mesa redonda

- Cita al par: el hallazgo 4 de `WO-02c-DESIGN.md` (editado en PARALELO por el fixer
  G3 durante este fix) dice "el default de compose según hallazgo 7/ficha 4.6" — esa
  referencia AHORA aterriza en el hallazgo 7 acotado por este fix: la cadena queda
  coherente (el default de compose sigue siendo CANONICAL_REPO; lo UNKNOWN es el
  runtime). Cero conflicto de contenido.
- Contaminación residual del claim sobre-clasificado en OTROS archivos: grep
  `default anvil|SIM_BACKEND=anvil` en el dir del audit = solo :142 y :221 del
  entregable (ambos corregidos) — 0 archivos adicionales.
- Precisión sobre el hint del charter: el cross-exam citó "§7/ficha 4.6"; "§7"
  corresponde al **hallazgo 7 del DESIGN** (la sección §7 del entregable es
  objetivo_usd y no contiene SIM_BACKEND). Ambos puntos corregidos igual.

## 6. Restricciones cumplidas

- Cero git/commit/push/PR/deploy (NO-GIT). Cero cargo/npm/build (restricción extra
  del run — doc-only fix, nada que compilar). Cero VPS (0 accesos ssh). Cero HTTP
  (0/5 requests). Cero .rs tocados; cero código gated modificado.
- Archivos tocados: `02c-SIM-STACK-SELECTOR.md` (2 ediciones quirúrgicas + errata
  §13 append-only), `WO-02c-DESIGN.md` (1 línea, hallazgo 7 — mismo WO-02c, no es
  archivo de otro owner), `GOAL-WORKORDERS.md` (entrada de cierre de este fix) y
  este reporte.
