# WO-02b-FIX-FMT — Desviación de formato de ficha DECLARADA (2026-09-17, fixer gang ronda 1)

> Fixer del Gang Omniscience, ronda 1. Doc-only. Cero .rs tocados, cero cargo/npm/build
> (restricción de run §36.4), cero git, cero VPS, cero HTTP (0/5 requests).

## 1. El hallazgo (charter)

El cross-examiner reportó: el gate de ficha del board exige las columnas **"misión (fase
pipeline)"** y **"estado muta/puro"** por operador; la ficha 02b no las tiene como tales —
la pureza está cubierta globalmente en §4 y la fase pipeline es implícita. Desviación
tolerable pero NO declarada como desvío. Hint del cross-examiner: línea en el header de §2
declarando la convención reducida y por qué (firma uniforme trait, pureza transversal §4),
o columna breve.

Nota fail-honest: el reporte cross-examiner que originó el hallazgo NO está en disco al
cierre de este fix (grep "tolerable|convención reducida|muta/puro" sobre el dir = 0 hits
en archivos cross). La cita proviene del charter del despacho. Precedente: WO-02b-FIX2
documentó el mismo patrón ("aserción de agente ≠ fact"). El gap en sí es verificable
directamente contra el board (líneas "Formato ficha", GOAL-WORKORDERS.md:85-87 en la
versión actual del board; el charter citaba :46-48 de la versión pre-append).

## 2. Fix aplicado (opción 1 del hint: línea en header de §2)

Archivo: `audits/first-understand-20260917/02b-OPERADORES-MATH-ENGINE.md`, §2
("Fichas op_01..op_32"), insertado inmediatamente después del bloque de convención por
columna, marcado `// WO-02b-FIX-FMT (2026-09-17, fixer gang ronda 1)`.

Contenido de la declaración (resumen):
- Las columnas "misión (fase pipeline)" y "estado muta/puro" NO se repiten por operador.
- Razón 1 — firma UNIFORME por trait sellado
  `TopologicalOperator::evaluate(&self, state:&MarketState) -> OperatorOutput`
  (mod.rs:97-114, re-verificado por este fixer con sed: trait decl en ese rango,
  `evaluate` toma `&self`, sin `&mut` en la interfaz). La "misión" de cada op ya es la
  columna central de su ficha (transformación entrada→salida). La fase pipeline es
  TRANSVERSAL a los 32: todos se consumen en las mismas 4 vías documentadas en §3
  (evidencia por combo, vector §IV 1..=31, RegimeRouter, ranking de riesgo op_32) — no
  existe fase diferenciada por operador que una columna pudiera poblar sin repetirse 32
  veces con el mismo valor.
- Razón 2 — pureza transversal cubierta GLOBALMENTE en §4 del mismo archivo: los 32
  PUROS, I/O sólo en periferia (math_evidence, live_risk_ranker, api.rs). Repetir "PURO"
  32 veces no aporta evidencia nueva.
- Fuente del hallazgo citada con la nota de que el reporte cross no está en disco.

Se eligió la línea de declaración sobre la "columna breve" porque la columna breve sería
32 celdas constantes (misma vía de consumo / mismo PURO) — ruido sin evidencia nueva,
contrario al espíritu fail-honest del board.

## 3. Verificación

- Evidencia de soporte re-verificada por este fixer ANTES de editar:
  - `backend/math-engine/src/operators/mod.rs:97-114` — trait `TopologicalOperator`,
    `fn evaluate(&self, state: &MarketState) -> OperatorOutput` (sed 95-116: confirmado).
  - Grep `&mut` en `operators/` (excl. tests y op_32_multi_objective congelado): los hits
    son `register_all` del registry (mod.rs:130, setup no-operator), RNG LOCAL de op_22
    (op_22_monte_carlo.rs:121, dentro de evaluate con `&self`) e internos de op_32
    (core.rs) — NINGUNO rompe el claim §4 "PUROS sin `&mut` en la interfaz / sin
    mutabilidad interior compartida". La cita a §4 queda respaldada tal como está.
  - §3 del mismo archivo documenta las 4 vías de consumo (convención R/C/V/A del §2).
- Post-edit: grep del marcador `WO-02b-FIX-FMT` en 02b = presente (1 hit en §2);
  el resto del documento sin cambios (Edit quirúrgico de un bloque).
- Board actualizado: entrada `// WO-02b-FIX-FMT` añadida en GOAL-WORKORDERS.md bajo el
  bloque WO-02b (después de WO-02b-FIX-N1).
- Doc-only: no aplica tsc/cargo/vitest (ningún archivo de código tocado); verificación
  = grep + lectura, consistente con la restricción de run del board (PROHIBIDO
  cargo/npm/build este run).

## 4. Mesa redonda / pares

- Construye sobre: `02b-OPERADORES-MATH-ENGINE.md` §1 (firma uniforme, mod.rs:97-114),
  §3 (vías de consumo), §4 (pureza global) — los tres bloques YA contenían la evidencia
  que justifica la convención reducida; el fix sólo la DECLARA como desvío del gate.
- No contradice ningún hallazgo previo. Archivos de otros WO NO tocados (02a/02c/02d
  usan formatos de ficha que SÍ incluyen muta/puro por pieza — 02d:8, WO-02c-DESIGN:9 —
  porque son fichas por ARCHIVO, no por operador; la desviación era exclusiva de 02b).
- Contaminación residual detectada: ninguna.
- Cero conflictos de claims con otros WO.
