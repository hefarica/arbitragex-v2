# WO-02c-FIX-G4 — Errata tally selector-api "12/12" → 13/13 · 2026-09-17

> Fixer gang ronda 1 (Gang Omniscience). Gap asignado: **G4 (menor)** del charter —
> "tally del verify 'selector-api COMPLETO (12/12)' es off-by-one — ground truth: 13
> archivos no-test en selector-api/src". Fuente del gap:
> `WO-02c-CROSS-EXAM.md:125` + `:173` (G4, agent-fixable, "nota en el adendum") y
> GAP-1 de `WO-02c-verify-CROSS-EXAM.md:32-42` (misma cifra, misma corrección).
> Hint del cross-examiner: nota correctiva append-only en el adendum §10.5 — CUMPLIDO.

## 1. Ground truth re-derivado (RULE 00 — no heredado del charter ni del cross-exam)

Comando reproducible, ejecutado por este fixer:

```
cd backend/selector-api && find src -type f ! -name '*.test.ts' | wc -l   → 13
```

Los 13 archivos (find completo, árbol principal de trabajo):

```
src/consumer.ts                        src/policy/engine.ts
src/index.ts                           src/score.ts
src/persistence.ts                     src/scoring/bayesian.ts
src/policy/blacklist.ts                src/scoring/engine.ts
                                       src/scoring/factors.ts
src/token_safety/cache.ts   src/token_safety/client.ts
src/token_safety/goplus.ts  src/token_safety/internal_heuristic.ts
```

Cobiertos por las fichas §5.1–5.9 de `02c-SIM-STACK-SELECTOR.md` — mapeo exacto:
5.1 index · 5.2 consumer · 5.3 policy/engine · 5.4 policy/blacklist ·
5.5 scoring/{engine,factors} (×2) · 5.6 scoring/bayesian · 5.7 token_safety/* (×4) ·
5.8 persistence · 5.9 score = **13/13**. **La conclusión de cobertura (COMPLETO)
SOBREVIVE; solo el conteo estaba mal.** Coincide con la lista nominal del cross-exam
(`WO-02c-verify-CROSS-EXAM.md:33-35`) — ahora doble-verificado.

## 2. Origen del "12" — UNKNOWN (fail-honest, no especulado)

`lines-per-file.txt` tiene DOS árboles para selector-api/src y **ninguno produce 12**:
- Árbol principal (:9141-9161): 21 archivos, 8 `*.test.ts` → **13** no-test.
- Worktree stale `.claude/worktrees/wf_257a24e1-859-2/` (:4896-4914): 19 archivos,
  6 `*.test.ts` (sin `sel-gate01.test.ts` ni `internal_heuristic.test.ts`) → **13** no-test.

Por tanto el hazard de doble árbol que el cross-exam §3 (:76-79) propuso como
"probable fuente de los tallies erróneos de GAP-1" NO explica este caso (sí pudo
explicar el sim-ctl 15/16). Clasificación: **UNKNOWN** — desliz de conteo manual del
verificador, sin artefacto intermedio reproducible. Irónico frente al estándar de
evidencia §34.5.3 que el propio verify aplicó a la cifra "87%" (C2, §10.4) y no aplicó
a su propio tally — ironía ya apuntada por el charter y registrada en ambas erratas.

## 3. Fix aplicado (2 archivos bajo claim WO-02c + board; cero código de producción)

| archivo | cambio | modo |
|---|---|---|
| `02c-SIM-STACK-SELECTOR.md` §10.5 (:350) | Nota correctiva append-only `// WO-02c-FIX-G4` inmediatamente debajo del texto original del verificador (que queda INTACTO encima — disciplina append-only del adendum). Contiene ground truth, comando, mapeo 13/13, origen UNKNOWN, ironía C2, y anota que la parte sim-ctl del mismo §10.5 ya está superseded por FIX-G2 (§12.3: 16/16) | append-only |
| `WO-02c-verify-VERIFY.md` §6 (:86) | Tally corregido in situ "12/12" → "13/13" con marcador inline (reporte propio del gang; corrección expresamente autorizada por el fix recomendado del GAP-1 del cross-exam :40-42) + anotación "sim-ctl 15/16" → superseded por FIX-G2 | in situ + registro |
| `WO-02c-verify-VERIFY.md` §9 (:111, nuevo) | Errata append-only de registro con toda la evidencia del §1-§2 de este reporte | append-only |
| `GOAL-WORKORDERS.md` (board) | Entrada `// WO-02c-FIX-G4 APLICADA` bajo WO-02c | append-only |

## 4. Verificación del fix (post-edit)

- Grep `WO-02c-FIX-G4`: presente en los 3 archivos corregidos (VERIFY :86/:111,
  entregable :350-351, board). Grep `12/12` en el árbol de auditoría: queda SOLO en
  los reportes del cross-examiner (WO-02c-CROSS-EXAM.md:125/:173 — archivos del
  reportero, cita verbatim del error, intocables por disciplina de claims) y en
  WO-02d-DESIGN.md:68 (12/12 de un contexto DISTINTO: checklist pre_execute_checklist
  — NO es este tally, verificado).
- Conteo re-ejecutado post-edit: 13 (idempotente, el fix no toca selector-api/src).
- Sin colisión con pares: el fichaje §5.x no fue tocado; FIX-G1 (§11) y FIX-G2 (§12)
  del entregable permanecen íntegros (grep de sus marcadores OK).

## 5. Restricciones

Cero git/cargo/npm/build/VPS/HTTP (0/5 requests). Solo .md de auditoría + board —
no hay código de producción bajo edit (protocolo NO-GIT OK). §32/§33/§34.3 intactos
(nada que firme/broadcastee). Lexicon OMEGA respetado.

## 6. Post-script de sincronía (ediciones paralelas detectadas al cierre)

Un fixer paralelo (**WO-02c-FIX-G5**, mitad sim-ctl del mismo GAP-1 del cross-exam)
editó los mismos archivos mientras este fix corría:
- Corrigió IN SITU la línea del tally en §10.5 del entregable (:339, "12/12"→"13/13"
  bajo marcador `// WO-02c-FIX-G4`) — CONSISTENTE con mi corrección, cero conflicto;
  mi nota append-only (:353+) provee la evidencia y el comando reproducible.
- Refutó la cadena aritmética "16/16" de FIX-G2 (pre-G2 = 14/16, post-G2 = 15/16,
  build.rs sin ficha; erratas §10 de VERIFY y §13 del entregable) y enmendó el último
  párrafo de mi nota §10.5 en esa dirección. Aceptada: añadí reconciliación
  append-only al final de mi errata §9 de `WO-02c-verify-VERIFY.md` para que ningún
  lector propague el "16/16" que mi texto original transcribía de §12.3.
- Las líneas citadas en §3/§4 de este reporte pueden haber derivado ±3 líneas por
  esas ediciones paralelas (disciplina append-only: los marcadores
  `// WO-02c-FIX-G4` / `// WO-02c-FIX-G5` son el ancla estable, no el número de línea).

