# WO-02a-FIX-R1 — Gap §9 objetivo_usd corregido (2026-09-17, FIXER ronda 1)

> Charter: corregir el gap del cross-exam sobre §9 de WO-02a-DESIGN.md: (1) lista de
> knobs USD declared-only con 0 consumidores, (2) cita de net_bps_ranking.rs como
> consumidor real de min_net_bps, (3) reconciliación con 02d (trading_config.capital_usd).
> Entregable: ADENDUM §9.1 append-only en `WO-02a-DESIGN.md` (marcado WO-02a-FIX-R1).
> Restricciones: 0 cargo/git/VPS/HTTP (cumplido — solo Read/Grep/Edit markdown).

## 1. Cambio aplicado

- `WO-02a-DESIGN.md` — insertado `### 9.1 ADENDUM (WO-02a-FIX-R1, 2026-09-17)` entre el
  cierre del §9 y el §10. Append-only: ninguna línea previa modificada ni borrada.
- `WO-02a-verify-VERIFY.md` NO tocado (archivo del verificador; adendum G1/G2 queda a su
  owner/orquestador — claims de archivo respetados). Cero conflictos con otros WO.

## 2. Evidencia recomputada (todo CANONICAL_REPO, greps re-ejecutables)

**A. 4 knobs USD declared-only** (grep field-access
`\.(min_ev_usd|max_gas_usd|min_size_usd|min_pool_liquidity_usd)\b` en `backend/
--include=*.rs` fuera de canonical_knobs.rs = **0 hits**):
- `min_pool_liquidity_usd`=150_000 — canonical_knobs.rs:57/:176/:278-281/:417-419
- `max_gas_usd`=120 — :60/:179/:287/:414-416
- `min_size_usd`=10_000 — :63/:182/:290/:414-416
- `min_ev_usd`=25 — :65/:183/:291/:414-416
Único wiring = observabilidad: main.rs:367-386 (boot → Redis `arbx:config:canonical_knobs`)
+ api-server canonical-knobs.ts (GET fail-honest). Mismo precedente declarativo que
`beam_k` (canonical_knobs.rs:74-82). Colisión de nombres documentada: trading_config.rs:164
(`max_gas_usd: Option<f64>` operador, también 0 field-access) y .rhai:44 (default 50.0,
capa cartucho) NO son este knob.

**B. min_net_bps — consumidor real = net_bps_ranking.rs**, con matiz MÁS estricto que el
hint del cross-exam:
- Contrato PASS `AND(T>0, U>=01_CONFIG!$B$13)` → `passes(min_net_bps)`: net_bps_ranking.rs:19,
  impl :141-143; const `DEFAULT_MIN_NET_BPS=5.0` cross-pinned al knob: :37-38 (test :347-351).
- Hot-path VIVO = la MÉTRICA como clave de ORDEN: `rank_by_net_bps` en
  orchestrator.rs:1019 (sort sized_batch) y discovery_workload.rs:358.
- **PERO `passes()` tiene 0 call-sites de producción** (solo tests :286-316 tras cfg(test)
  :188): el gate V≥B13 del workbook NO se invoca hoy fuera de tests; el piso económico
  efectivo sigue siendo `GasFloorBreach` (size_optimizer.rs:85). El hint decía "consumo
  real vía passes(min_net_bps)" — refutado en lo angosto y documentado fail-honest.

**C. Reconciliación 02d** — fuentes USD runtime REALES verificadas: `capital_usd`
(trading_config.rs:183; cap 1/50 = 2%, execution_admission.rs:193-201 leído directo),
`min_profit_usd` (:260 gate; per-estrategia `effective_min_profit_usd` :622),
`simulation_capital_usd` (:166), `simulation_target_profit_usd` (:249, stores-not-gates).
scanner.rs:2568 `min_profit_threshold: cfg.min_profit_usd` confirmado en hot-path.

## 3. Framing §9 corregido (el que el board debe consumir)

El hot-path de searcher-rs opera con: (1) umbrales RELATIVOS vivos (net_bps ranking +
gas-safety floor), (2) un piso USD absoluto operador-configurable vivo
(`trading_config.min_profit_usd`, PG runtime), y (3) 4 knobs USD workbook DECLARED-ONLY
(canonical_knobs.rs:57-65, precedente beam_k). **El GAP objetivo_usd POR ESTRATEGIA
SOBREVIVE**: ningún `objetivo_usd`/`target_usd` real; lo más cercano canónico es
`simulation_target_profit_usd` (filtro UI, R8 stores-not-gates).

## 4. Verificación

- Markdown-only: no aplica cargo/tsc (restricción board WO-02 de todos modos los prohíbe).
- Marcador presente: grep `WO-02a-FIX-R1` en WO-02a-DESIGN.md = 1 hit.
- 100% de las file:line del adendum re-abiertas y confirmadas antes de escribir
  (incl. las heredadas del cross-exam: :249/:260/:622/scanner:2568 — verificadas, no
  propagadas a ciegas; precedente "aserciones de agentes ≠ facts").
