# WO-02c-FIX-G3 — Cualificación del hard finding 4 (idempotencia revm-scoped) · 2026-09-17

> Fixer del Gang Omniscience, ronda 1. Gap: G3 del cross-exam de WO-02c
> (WO-02c-CROSS-EXAM.md §2-G3) — hallazgo NUEVO del cross-examiner.
> Charter del fix (hint del cross-examiner, seguido literal): "Cualificar el
> hallazgo 4 en WO-02c-DESIGN.md y ficha 4.5: 'válido solo para simulator=revm
> (B2c); path anvil legacy sin dedup'. Extender la dedup a anvil, si se desea,
> es WO propio con diff, operator-gated."
> Método: Read/Grep/Bash-readonly. CERO cargo/npm/build (board :17-19), cero
> git, cero VPS, cero HTTP (0/5 requests), cero código de producción tocado.

## 1. El gap

El hard finding 4 del DESIGN enunciaba "Idempotencia de redelivery:
`insert_simulation` ON CONFLICT DO NOTHING + skip de XADD duplicado" como
verdad canónica de wiring INCONDICIONAL. El índice árbitro es PARCIAL
(`WHERE simulator = 'revm'`) a propósito: el path legacy anvil (el default de
compose según la propia ficha 4.6/§7) NO deduplica → `inserted_fresh`
siempre true → un PEL-redelivery de una sim anvil passed re-publicaría
duplicado a `arbx:opps:simulated`. Impacto HOY nulo (passed=true = 0 en toda
la historia) pero la ficha se consumía como canónica por WO-06/gates.

## 2. Evidencia re-verificada de primera mano (RULE 00 — nada heredado)

| claim | evidencia file:line | lectura |
|---|---|---|
| índice árbitro PARCIAL | `database/migrations/113_simulations_revm_idempotency.sql:27-29` | `CREATE UNIQUE INDEX ... ON simulations (opportunity_id) WHERE simulator = 'revm'` — verificada |
| "PARTIAL on purpose" | mismo archivo :12-14 | "legacy anvil history legitimately holds multiple attempts per opportunity" — verificada |
| diseño original multi-attempt | `database/migrations/004_simulations.sql:2` | "One simulation per attempt; an opportunity may have multiple (e.g., retried with tweaked params)" — verificada |
| ON CONFLICT revm-scoped | `backend/sim-ctl/src/persistence.rs:35` | `ON CONFLICT (opportunity_id) WHERE simulator = 'revm' DO NOTHING` — fila anvil nunca arbitra → `Ok(true)` siempre — verificada |
| path legacy produce Anvil | `backend/sim-ctl/src/sim_engine.rs:163,205` | `simulator: SimulatorKind::Anvil` — verificada |
| Revm solo en B2c | `canonical_plan_consumer.rs:205`, `consumer.rs:660`, `revm_backend.rs:193-243` | grep `SimulatorKind::Revm` — verificada |
| gate XADD + intención anti-double-count | `backend/sim-ctl/src/consumer.rs:443-463` | comentario ":443-450" + `sim.passed && inserted_fresh` :463 — verificada |
| impacto hoy nulo | `audits/real-cycles-audit-20260916/00-SYNTHESIS.md` | passed=true = 0 histórico (cita del cross-exam, consistente con la ficha 4.6/§5.3 del propio 02c: 100% rejected) |

Clasificación: CANONICAL_REPO (migraciones 004/113 + sim_engine.rs +
persistence.rs + consumer.rs citados y releídos).

## 3. Fix aplicado (doc-only, quirúrgico, marcado `// WO-02c-FIX-G3 (2026-09-17)`)

1. **`WO-02c-DESIGN.md` hallazgo 4** — reclasificado
   `[CANONICAL_REPO]` → `[CANONICAL_REPO — scope ACOTADO por // WO-02c-FIX-G3]`
   con cadena de evidencia completa + nota "extender la dedup a anvil = WO
   propio con diff, operator-gated".
2. **Ficha 4.5 (`persistence.rs`)** en `02c-SIM-STACK-SELECTOR.md` — nota
   inline: idempotencia revm-scoped POR DISEÑO, contraejemplo de double-publish
   del path anvil bajo PEL-redelivery, impacto hoy nulo.
3. **Ficha 4.2 (`consumer.rs`)**, mismo archivo :119 — tercera ocurrencia del
   claim sobre-generalizado ("idempotente por redelivery") acotada a "solo
   filas simulator='revm'" con remisión a 4.5 (contaminación
   intra-entregable cerrada, no exigida por el hint pero misma sobre-generalización).
4. **Errata §14 append-only** en `02c-SIM-STACK-SELECTOR.md` (secciones
   14.1 evidencia / 14.2 cambios / 14.3 sincronía).

## 4. Lo que NO se tocó (verificado inocente o gated)

- `02c-SIM-STACK-SELECTOR.md:278` (traza E2E §10.2): describe el gate
  `passed && inserted_fresh` tal como el código lo escribe — factual, no
  sobre-generalizado.
- `02c-SIM-STACK-SELECTOR.md:260` (matriz §8 fila 4.5): citas file:line del
  verify — factual.
- `WO-02c-verify-VERIFY.md`: grep "idempoten" = 0 ocurrencias del claim (solo
  nota de drift ±2 líneas :20) — sin contaminación que corregir.
- **Código de producción** (`persistence.rs`, `consumer.rs`, migraciones 004/
  113): INTOCADO. Extender la dedup a anvil es decisión de diseño del
  operador: rompería la semántica multi-attempt-diagnostics de la era anvil
  que las migraciones documentan explícitamente (113:12-14, 004:2). WO propio
  con diff exacto, operator-gated (consistente con G6/G7 del cross-exam).

## 5. Sincronía de mesa redonda

- Pares leídos ANTES: GOAL-WORKORDERS.md completo, WO-02c-CROSS-EXAM.md
  (origen del gap), WO-02c-DESIGN.md, 02c-SIM-STACK-SELECTOR.md (fichas 4.2/
  4.5/4.6, erratas §11-§13).
- **Convergencia con el fixer G5** (errata §13): su análisis ya anticipaba
  que "G3 (dedup parcial WHERE simulator='revm') pasa a ser el camino caliente
  si el .env dijera revm" — este fix hace load-bearing ese matiz en ambas
  direcciones: con SIM_BACKEND=revm la dedup es el camino caliente EXACTO;
  con el default anvil el branch caliente corre SIN dedup. Cero contradicción.
- **Colisión con fixers paralelos G1/G2/G4/G5**: NINGUNA. Secciones disjuntas
  (mis edits: hallazgo 4 del DESIGN, fichas 4.2/4.5, errata §14 nueva). Los
  archivos cambiaron en disco durante el fix (ediciones paralelas observadas);
  cada uno de mis 4 edits aplicó limpio sobre contexto propio y el grep
  post-edit muestra los marcadores G1/G2/G4/G5 intactos junto a los míos.
- Cero archivos de otro WO tocados (ambos .md son del claim WO-02c).

## 6. Cumplimiento de reglas duras

- RULE 00 / R8: evidencia re-leída de primera mano; passed=0 citado como
  impacto nulo, no como ausencia de gap.
- §32/§33: read-only, sin executor/wallets/capital/firma/broadcast.
- §34.3: intocado (la extensión de dedup quedó operator-gated).
- NO-GIT: cero commit/push/PR/deploy (verificable: solo 2 .md del audit dir
  modificados, ambos untracked).
- Restricción extra del run: cero cargo/npm/build.
- HTTP: 0/5 requests.

## 7. Pendiente para el operador (gated, NO aplicado)

1. Decidir si el matiz G3 entra como observación/wire-audit en un WO
   (pregunta 4 del cross-exam) — este fix ya lo deja documentado como
   candidato `sim_consumer.anvil_redelivery_dup_publish` en la errata §14,
   análogo al candidato `unpinned_replay` del fixer G2.
2. Extender la dedup a anvil (si se desea exactamente-once también ahí) = WO
   propio con diff sobre persistence.rs/consumer.rs + decisión sobre la
   semántica multi-attempt de las migraciones 004/113.
