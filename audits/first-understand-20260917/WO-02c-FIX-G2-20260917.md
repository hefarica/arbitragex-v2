# WO-02c-FIX-G2 — Ficha route_lookup.rs + hallazgos a la matriz §8 — 2026-09-17 (fixer gang, ronda 1)

> Charter: cerrar **G2** de `WO-02c-verify-CROSS-EXAM.md:88-97/:171`. El verify de 02c
> calificó la ausencia de ficha de `backend/sim-ctl/src/route_lookup.rs` (232 LOC,
> núcleo del enriquecimiento A3) como "OMISIÓN REAL, la de mayor peso" (§10.5 del
> adendum) y el board gate (GOAL-WORKORDERS.md:16) exige "cada ficha con
> ruta+firma+prueba (línea exacta) o marcada GAP" — WO-02c estaba DONE sin ninguna de
> las dos. Además, los 2 hallazgos nuevos del verify (doc-drift :15; unpinned_replay)
> vivían solo en el adendum, fuera de la matriz de destinos §8.
>
> Método: Read/Grep/`wc -l` SOLAMENTE (restricción board :17-19: cero cargo/npm/build).
> Cero git, cero VPS, 0/5 requests HTTP. Lexicon OMEGA respetado.
> Pares leídos ANTES de actuar: GOAL-WORKORDERS.md completo, WO-02c-CROSS-EXAM.md,
> WO-02c-verify-VERIFY.md, 02c-SIM-STACK-SELECTOR.md completo (incluida la errata §11
> del fixer G1, que aterrizó en paralelo durante este fix).

## 1. Qué se aplicó (2 ediciones quirúrgicas + este reporte)

1. **Ficha 4.9 de route_lookup.rs** — anexada como **§12.1** en
   `02c-SIM-STACK-SELECTOR.md` (append-only; ver §4 por la renumeración). Formato
   board completo: ruta | firma | semántica Err/None | misión | merge_decimals |
   op_origen | dependencias inversas | estado | prueba file:line | destino ACTIVO |
   objetivo_usd GAP.
2. **4 filas aditivas en la matriz §8** del mismo archivo (cero filas existentes
   reescritas): ficha-row de route_lookup.rs (ACTIVO) + los 2 hallazgos del verify
   (doc-drift :15 = GAP documental; unpinned_replay = observación no-R8) + 1 hallazgo
   NUEVO propio (doc-drift :159-162, GAP documental).
3. **Entrada en el board** (`GOAL-WORKORDERS.md`, append-only al final).

## 2. Síntesis de la ficha (canónico para la mesa)

- **Firma**: `fetch_candidate_inputs(&PgPool, Uuid) -> Result<Option<CandidateInputs>, sqlx::Error>`
  (route_lookup.rs:113-116); `merge_decimals` pura (:60); `resolve_route_decimals`
  (:82-86, tabla `tokens` mig 098, `WHERE chain_id=$1 AND lower(address)=ANY($2) AND
  decimals IS NOT NULL` :88-94).
- **Semántica Err vs Ok(None) (material de traza E2E)**:
  - `Ok(Some)` = fila existe Y `route_metadata.is_populated()` (:129-137; def en
    shared-rs/candidates.rs:188).
  - `Ok(None)` = fila ausente (:130) O route_metadata vacío/`{}` (:132-134) — R8:
    nunca fabrica topología. Caller: `counted_gap("route_metadata_not_available")`
    (consumer.rs:536-541) — gap terminal tipado, no PEL.
  - `Err` = error de PG (query :117-127 / resolve :86-100). Caller: `return Err(...)`
    (consumer.rs:542) → **PEL retry**, no dead-letter — coherente con `ghost_verdict`
    (:746-766): Err ≠ confirmado ausente.
  - La distinción es exactamente el anti-patrón que SIMWIRE-02c P1-1 documentó
    (`.ok().flatten()` muerto por tratar outage como fila fantasma).
- **Dependencias inversas** (grep re-ejecutado): consumer.rs:534 (path stream B2c) y
  main.rs:208 (path HTTP A3) — "same source" declarado en consumer.rs:533. Prueba con
  PG vivo: `sim-ctl/tests/simwire02_route_aware.rs` (incluye el módulo por `#[path]`
  :36-37; llama `fetch_candidate_inputs` :138-147).
- **merge_decimals**: tokens-table primero, overlay route_metadata al final ("la claim
  de la fila gana", :67-70); skip honesto de decimals fuera `0..=255` (:62-66) —
  SMALLINT puede guardarlos, u8 no; jamás truncar a basura (tests :188-231).
- **Destino: ACTIVO** (2 call-sites productivos + prueba de integración viva).
  objetivo_usd: GAP (hereda §7, grep 0 hits).

## 3. Los 3 hallazgos ahora en matriz §8

1. **doc-drift :15 (verify §10.2)** — header del módulo dice "returns `None` on PG
   error"; la impl devuelve `Err` (:116-127). Comportamiento CORRECTO (Err→PEL); el
   header miente. Corrección de 1 línea en código = **gated operador, NO aplicada**.
2. **unpinned_replay (verify §10.2)** — block_number NULL en fila PG → candidate sin
   pin (consumer.rs:608 `.filter(...).map(...)`) → `simulator_for_candidate` :780-783
   devuelve el simulador compartido sin pin → replay contra `latest`, sin log.
   Observación de determinismo (NO defecto R8 — consistente con convención SimulatorV2
   None=latest). Candidato `sim_consumer.unpinned_replay` debug-log, **gated**.
3. **doc-drift :159-162 (NUEVO de este fixer)** — el comentario del bloque `#[cfg(test)]`
   cita `sim-ctl/tests/route_lookup_integration.rs` como "the live-PG test (CI runs it
   under the integration-tests job)". **Ese archivo NO existe** (grep del árbol
   completo: la única ocurrencia del nombre es la propia cita). La cobertura real vive
   en `simwire02_route_aware.rs` (misma función, PG vivo). Drift de PUNTERO, no de
   cobertura — la prueba existe bajo otro nombre. Corrección **gated**.

## 4. Colisión con paralelo (documentada, no pisada)

El fixer G1 anexó su errata como §11 en `02c-SIM-STACK-SELECTOR.md` mientras este fix
estaba en vuelo (el archivo cambió en disco entre mi lectura y mi primera edición).
Resolución: mi sección pasó a **§12** y corregí mi propia fila §8 (decía "§11.1" →
"§12.1"). Cero contenido de G1 tocado; sus cambios (re-bajo de la cifra "87%" en §5.3
y §9.4) conviven con los míos. Sin otros conflictos de claim: route_lookup.rs no está
fichado por ningún otro WO (02a anexó pendientes distintos: adendum §7 en
WO-02a-verify-VERIFY.md y doc-comment impact_index.rs:21-22 — archivos que este fix NO
tocó).

## 5. Verificación del fix (goal-driven)

- Criterio: board gate "ficha con ruta+firma+prueba (línea exacta)" + "2 hallazgos en
  matriz §8".
- Ejecutado: ficha §12.1 presente con ~20 marcadores file:line, TODOS re-abiertos por
  este fixer con Read/Grep (no heredados del verify): 232 LOC `wc -l`; :15, :60, :67-70,
  :82-86, :88-94, :104-108, :113-116, :117-127, :129-137, :159-162; consumer.rs:533-543,
  :608, :746-766, :776-784; main.rs:17, :208; shared-rs/candidates.rs:188;
  database/migrations/098+099; tests/simwire02_route_aware.rs:36-37/:138-147.
- Matriz §8: 4 filas nuevas con prefijo `// WO-02c-FIX-G2 (2026-09-17)`, tabla sigue
  renderizando (4 columnas intactas, filas previas byte-idénticas).
- Tally heredado actualizado: sim-ctl 15/16 → **16/16** fichado (build.rs y
  sim-core/lib.rs siguen declarados como omisiones menores por el verify §10.5).
- Reglas duras: RULE 00 (todo marcador re-verificado antes de citarlo; el hallazgo
  nuevo declara "no existe" con grep como evidencia); §32/§33 (cero executor/wallet/
  broadcast; ni siquiera se tocó VPS); NO-GIT (cero commit/push); restricción extra del
  run cumplida (cero cargo/npm/build); diffs marcados con el ID del WO.
