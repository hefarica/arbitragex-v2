# WO-02b-FIX-N1 — Errata notación op_15 "[0, r_in]" → "[0, r0]" (fixer gang, ronda 1 · respawn-A) · 2026-09-17

> Gang Omniscience · rol: fixer (reemplazo-A del agente caído; mitad A del charter).
> Kind: errata documental 100% markdown — nada que compilar (restricción de run §36.4
> igualmente cumplida: cero cargo/npm/build). Cero git. Cero VPS (ni lectura hizo falta).
> Diffs marcados `// WO-02b-FIX-N1 (2026-09-17)`. Archivo corregido:
> `02b-OPERADORES-MATH-ENGINE.md` — archivo del claim de WO-02b, sin conflicto de
> ownership con otros WO. Nota de concurrencia: mi par respawn-B editó el MISMO archivo
> en paralelo (su adenda aterrizó como §9); mi errata aterrizó como §10 y el puntero
> in-situ apunta a §10 — coexistencia append-only verificada.

## 1. Gap asignado (cross-examiner, ronda 1)

Discrepancia interna no reconciliada en op_15: la ficha del diseñador dice bracket
**[0, r_in]** mientras el código y el verificador dicen **[0, r0]**; el verificador
corrigió silenciosamente sin flaggear la divergencia del diseñador. Hint: unificar a
[0, r0] (reserva del token de entrada) o declarar `r_in := r0` explícitamente.

Nota de línea: el charter citaba "02b line 91" — la fila real de la ficha op_15 está en
la línea 100 del archivo de hoy (drift de numeración por ediciones append-only previas
de pares; fail-honest, declarado). La fila del verificador PASS está en la sección
VERIFICACIÓN (~línea 201 pre-mis-edits).

## 2. Verificación independiente ANTES de editar (RULE 00 — no heredo el hint)

- Código: `op_15_golden_section.rs:113-115` — `// Maximización por sección áurea sobre
  [a, b] = [0, r0].` con `let mut b = r0;` — CANONICAL_REPO.
- Semántica de r0: `:85` — `let (r0, r1) = state.liquidity_reserves[0];`; la función
  objetivo `f(x)=r1·γ·x/(r0+γ·x)−x−gas` (:104-110) es getAmountOut CPMM con x del
  token de entrada contra reserves (r0 in, r1 out) ⇒ r0 = reserva del token de entrada.
  Confirmación adicional: tolerancia escalada al bracket `1e-9 * r0` (:120),
  `metadata["r0"]` (:165) y `vector_result=[x_star, f_star, r0]` (:172).
- Tests: `src/operators/real_ops_tests.rs` bloque op_15 — comentario del propio test
  "vector_result = [x*, f*, r0]; x* ∈ (0, r0]" con aserción `x_star <= 1_000_000.0`
  (pool_state r0=1e6). Tests, código y metadata usan r0, jamás r_in.
- Notación huérfana: `grep -n "r_in" op_15_golden_section.rs` = 0 matches; en TODO
  backend/math-engine solo falsos positivos de substring (`pair_index`,
  `for_inclusion`). `r_in` no existe como símbolo en ningún fuente — era notación
  exclusiva del diseñador en la ficha, sin alias declarado.

## 3. Corrección aplicada (02b-OPERADORES-MATH-ENGINE.md)

1. **Celda op_15 (§2, tabla Optimization, línea 100) in situ**: "golden-section sobre
   [0, r_in]" → `// WO-02b-FIX-N1 (2026-09-17) ... (golden-section sobre [0, r0],
   r0 = reserva del token de entrada, liquidity_reserves[0].0 :85, bracket :113-115 —
   antes decía [0, r_in], símbolo inexistente en el código, ver ERRATA §10)`.
2. **ERRATA §10 append-only** al final del archivo: preserva el texto original como
   procedencia, documenta el defecto (divergencia diseñador↔código + corrección
   silenciosa del verificador), la evidencia byte-exact y la decisión.
3. **Decisión (opción 1 del hint)**: unificar a [0, r0] en vez de declarar
   `r_in := r0`, porque r0 es el ÚNICO símbolo con existencia en código, tests y
   metadata — declarar un alias preservaría la notación huérfana. Cero cambio
   semántico: el bracket siempre fue [0, r0] en el ejecutable.
4. **Fila PASS del verificador**: NO tocada (ya decía [0,r0], que es correcto); la
   errata §10 ES el flag faltante de la divergencia que había absorbido en silencio.

## 4. Verificación de la corrección (post-edit)

- `grep -n "r_in" 02b-OPERADORES-MATH-ENGINE.md`: los únicos "r_in" vivos están como
  cita de procedencia (celda corregida línea 100 y errata §10) — la notación como
  aserción viva quedó en 0.
- Celda corregida presente en línea 100 con marcador `// WO-02b-FIX-N1 (2026-09-17)`.
- Errata §10 presente (línea ~403), numerada después de la §9 de mi par (concurrencia
  resuelta: puntero in-situ apunta a §10, verificado con grep).
- Ningún archivo .rs modificado: `git status --porcelain -- backend/math-engine` vacío
  (el código era correcto; el defecto era exclusivamente de la ficha).
- Coherencia de mesa post-edit: fila verificador [0,r0] + ficha [0,r0] + errata que
  documenta la historia = discrepancia interna RECONCILIADA.

## 5. Mesa redonda (sincronía)

- Board (GOAL-WORKORDERS.md) leído completo + reportes de pares relevantes:
  WO-02b-FIX-20260917.md (errata op_22, mismo archivo — construyo sobre su convención
  de marcadores append-only), WO-02b-verify-VERIFY.md:40 (fila op_15 del math-validator,
  origen de la corrección silenciosa aquí flaggeada), 02b §9/ADENDA de mi par respawn-B.
- Sin refutaciones a hallazgos previos: el PASS del verificador sobre la matemática de
  op_15 queda INTACTO (τ=(√5−1)/2 :119, maximización clásica :128-135 — re-verificado);
  este fix cierra el hueco de trazabilidad notacional, no el de matemática.
- Contaminación cruzada pre-edición: grep "r_in" sobre el dir de auditoría = único match
  la propia celda op_15 (falsos positivos verificados uno a uno: 02d:188
  `wait_for_inclusion`, WO-02a-DESIGN.md:496 `pair_index`, WO-02a-verify-VERIFY.md:88
  `pair_index`, lines-per-file.txt `pair_index`/`market_maker_inventory`). Ningún otro
  WO propagó r_in.
- Archivos de otros owners: NO tocados.

## 6. Reglas duras cumplidas

- RULE 00: toda la evidencia re-leída de fuente (.rs + tests), nada heredado del hint
  sin verificar; el "r_in" original se preserva solo como procedencia citada.
- §32/§33: cero executor/wallets/capital/broadcast; ni una request al VPS.
- NO-GIT: sin commit/push/PR — solo edición local de markdown + verificación grep.
- Sin cargo/npm/build (restricción del run; irrelevante para markdown, declarado igual).
- Presupuesto dominio público HTTP: 0 de 5 requests usados (fail-honest).
