# WO-02d-FIX-G1 · Conteo inflado shared-rs "33" → 24 — APLICADA y verificada

> Fixer gang Omniscience ronda 1 · 2026-09-17 · gap G-1 del charter
> (= DEFECTO 1 del cross-exam `WO-02d-verify-CROSS-EXAM.md` §2).
> Regimen: §32/§33 read-only, doc-only, cero git/cargo/npm/VPS/HTTP (0/5 requests).

## 0. Sincronía de mesa (estado al inicio)

- Board `GOAL-WORKORDERS.md` leído completo. Pares de la ronda 1 ya publicados y
  leídos para no duplicar/chocar: WO-02a-FIX-G1/R2/G3, ERRATA NUMÉRICA + CONVERGENCIA
  DUAL, WO-02b-FIX{,-MATHMAP,-VERIFY,2,-N1,-FMT,-DRIFT-COMPOSE},
  WO-02c-FIX-G1/G2/G3/G4/G5/TALLY.
- Fuente del gap: `WO-02d-verify-CROSS-EXAM.md` §2 ("conteo '33 archivos shared-rs'
  es FALSO (24 reales)") y su tabla de gaps fila 1 (agent-fixable).
- Este fix CONSTRUYE sobre el cross-exam: sus números fueron RE-DERIVADOS aquí
  (RULE 00), no heredados.

## 1. Evidencia ground-truth (recomputada por este fixer)

Comandos reproducibles ejecutados sobre el árbol de trabajo local:

```
$ cd backend/shared-rs
$ find src -name "*.rs" | wc -l          → 24
$ find . -name "*.rs" -not -path "./src/*" -not -path "./target/*" | wc -l → 1
$ find ../relays-client/src -name "*.rs" | wc -l → 19
```

- Los 24 archivos src (lista completa, sort): candidates.rs, chains.rs, config.rs,
  contracts.rs, cred_rotation.rs, db_pool.rs, flashloan_math.rs, health.rs,
  killswitch.rs, lib.rs, logging.rs, metrics.rs, oracle_snapshot.rs,
  oracle_snapshot/configured_rpc.rs, paper_mode.rs, pre_execute_checklist.rs,
  price_oracle.rs, risk_ledger.rs, rpc_failover.rs, settlement_risk.rs,
  sim_taxonomy.rs, token_identity.rs, tokens.rs, trading_config.rs.
- Crate-wide: +1 test fuera de src (`tests/oracle_rpc_selection.rs`) = 25.
- "19 relays-client": re-verificado CORRECTO (no se toca).
- Cobertura de la tabla §3 de la ficha: **19 filas → 23 de 24 archivos src**;
  `lib.rs` solo citado en el preámbulo de §3 (:11, verificado: doc-comment
  "Used by: searcher-rs, sim-ctl, relays-client, recon"); el test fuera de src
  no tiene ficha. Ninguna lectura produce 33 — coincide con el cross-exam §2.

## 2. Cambios aplicados (SOLO .md, todos marcados `// WO-02d-FIX-G1 (2026-09-17)`)

| Archivo | Localidad | Cambio |
|---|---|---|
| `02d-RELAYS-CLIENT-SHARED.md` | §3 header | "(módulo-tipo, 33 archivos)" → "(módulo-tipo, 24 archivos .rs en src/ — 25 crate-wide con tests/oracle_rpc_selection.rs)" + blockquote de procedencia con conteo canónico |
| `02d-RELAYS-CLIENT-SHARED.md` | §6 | "ACTIVO (33/33 módulos documentados a nivel módulo-tipo)" → "ACTIVO (23/24 archivos src documentados a nivel módulo-tipo en la tabla §3; lib.rs citado en el preámbulo :11; tests/oracle_rpc_selection.rs fuera de src, sin ficha)" |
| `02d-RELAYS-CLIENT-SHARED.md` | §ERRATA-01 (append-only, al final) | errata completa: claim original, realidad reproducible, origen del 33 = UNKNOWN, impacto, contaminación residual, alcance |
| `GOAL-WORKORDERS.md` | :51-52 (resumen DONE WO-02d) | "52 fichas: 19 relays-client + 33 shared-rs" → conteos reales + puntero a esta errata |
| `GOAL-WORKORDERS.md` | "Reglas de esta mesa" | entrada `// WO-02d-FIX-G1 APLICADA` (convención de los fixers 02a/02b/02c de esta ronda) |

Disciplina append-only respetada: el adendum de VERIFICACIÓN del WO-02d-verify y
todo el texto fáctico original quedan intactos; los cambios numéricos in situ
llevan marcador y la errata registra el valor previo.

## 3. Verificación del fix

1. `grep -n "33" 02d-RELAYS-CLIENT-SHARED.md` post-edit: las únicas ocurrencias de
   "33" restantes en la ficha son las CITAS DEL DEFECTO (marcador del fix, §ERRATA-01,
   y el texto original del adendum verify que no menciona 33) — ninguna presenta 33
   como cifra vigente de shared-rs.
2. Recompute post-edit de la cifra canónica del §3 header: 24/25 = reproduce con los
   comandos de §1 (ejecutados dos veces, mismo resultado).
3. Aritmética del board coherente: 19 relays-client (verificado) + shared-rs 24 src
   (verificado) — "52 fichas" eliminado por ser aritmética sobre un sumando falso
   (19+33); NO se sustituyó por "43 fichas" para no fabricar un nuevo tally de
   "fichas" (las filas de tabla ≠ archivos; 19 filas tabulares cubren 23 archivos).
4. `git status` de los archivos tocados: solo los dos .md de este fix + este reporte
   (cero .rs, cero commit — protocolo NO-GIT cumplido).

## 4. Contaminación residual (documentada, NO pisada — archivos de otros WO)

| Archivo | Línea | Contenido | Decisión |
|---|---|---|---|
| `WO-02a-verify-VERIFY.md` | :16, :309 | "02d-RELAYS-CLIENT-SHARED.md (existe, 52 fichas…)" | owner = verificador 02a; su errata pendiente es propia (board ya registra adenda §7 pendiente para ese archivo). NO tocado. |
| `WO-02c-CROSS-EXAM.md` | :68 | "(verificado: 02d fichó los 33 shared-rs)" | owner = cross-exam 02c; es cita lateral de contexto, no claim de cobertura propia. NO tocado. |
| `WO-02d-verify-CROSS-EXAM.md` | :43-47 | cita "33" como EVIDENCIA del defecto | INTENCIONALMENTE intacto: es el registro del refutador; editarlo destruiría la trazabilidad del gap. |

Si la mesa quiere descontaminar 02a-verify/02c-cross: mini-errata append-only de
sus owners apuntando a este reporte (1 línea c/u).

## 5. Fuera de alcance (declarado)

- **DEFECTO 2 del cross-exam (TTL carrier validated_plan 300 s vs 60 s, §4 de la
  ficha)**: gap separado en la tabla del cross-exam (fila 2, agent-fixable) — el
  charter de ESTE fixer es solo G-1. NO tocado; queda para su propio fixer.
- Origen exacto del "33": UNKNOWN. Hipótesis registrada en la errata (aritmética
  auto-consistente 19+33=52 sin fuente) — FAIL-HONEST, no se especula más.

## 6. Clasificaciones

- Cifra 24 src / 25 crate-wide: PRIMARY_SOURCE (find propio, doble ejecución).
- "19 relays-client": PRIMARY_SOURCE (find propio, coincide con cross-exam §2).
- Origen del 33: UNKNOWN (declarado).
- Impacto del defecto original: cero seguridad; contaminación de censo (/goal).

— FIXER WO-02d-FIX-G1 · fin.
