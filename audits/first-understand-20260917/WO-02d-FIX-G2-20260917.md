# WO-02d-FIX-G2 · TTL del carrier `arbx:validated_plan` — 300 s (scanner) / 60 s (candidate_simulation)

> Fixer gang ronda 1 · 2026-09-17 · DEFECTO 2 del cross-exam
> (`WO-02d-verify-CROSS-EXAM.md` §3, gap "agent-fixable" de su tabla §5).
> READ-ONLY §32/§33: cero código, cero git, cero cargo/npm/build (restricción
> board WO-02), cero VPS, cero HTTP (0/5).

## 0. Sincronía de mesa

- Board `GOAL-WORKORDERS.md` leído completo. Pares relevantes leídos: `02d-RELAYS-CLIENT-SHARED.md`
  (entregable corregido), `WO-02d-verify-CROSS-EXAM.md` (origen del gap),
  `WO-02d-FIX-G1-20260917.md` (fixer del DEFECTO 1, dejó este gap explícitamente
  "pendiente de su propio fixer"). Fixer paralelo activo detectado durante el fix:
  **WO-02d-FIX-GRAN** (obs. 4.1 del cross-exam, granularidad §3) — coordinación abajo (§5).

## 1. Gap

Ficha §4 (`02d-RELAYS-CLIENT-SHARED.md`): "Key `arbx:validated_plan:<id>` (**TTL 300 s**)",
con columna productor listando 3 entidades (canonical_plan_consumer + scanner +
candidate_simulation). El TTL real del key NO es uniforme: un escritor usa EX 300,
el otro EX 60. El fail-closed del terminus queda intacto, pero la ventana
detect→broadcast de la ruta candidate_simulation es 60 s y el documento la describía mal.

## 2. Evidencia re-derivada de primera mano (RULE 00, no heredada del cross-exam)

Escritores del key = exactamente **2** (grep `arbx:validated_plan` sobre backend/ completo;
los únicos otros hits son lectores y el prefijo constante):

1. `backend/searcher-rs/src/scanner.rs`
   - key `format!("arbx:validated_plan:{}", opportunity.id)` :2449
   - `redis::AsyncCommands::set_ex(redis, &key, plan_json, 300u64)` :2458 → **TTL 300 s**
   - comentario :2452-2453: "TTL 300s comfortably covers the detect→broadcast window
     (longer than the 180s pending-tx backstop in submit_engine)"
   - fail-SOFT :2459-2469 (warn y continúa; consumer fail-CLOSED — asimetría documentada :2445-2447).
2. `backend/searcher-rs/src/candidate_simulation.rs`
   - `persist()` :572-597; pipe atómico :579-593; `.arg("EX").arg(60)` :584-585 → **TTL 60 s**
     (la cita ":586" del cross-exam apunta a `.ignore()` :586 — off-by-one corregido aquí)
   - el MISMO pipe escribe `arbx:validated_economics:<id>` también EX 60 (:587-592)
   - call-site: `backend/searcher-rs/src/opportunity_emitter.rs:367` — ruta de admisión
     de candidates (permit → prepare → persist; doc-comment :570-571: "Plan and economic
     evidence become visible together before the opportunity UUID is published").

`backend/sim-ctl/src/canonical_plan_consumer.rs` = **CONSUMIDOR, no escritor**:
- `CARRIER_KEY_PREFIX` :41 define el esquema del key (comment :39-40 lo dice: "Same key
  scheme the producer writes (scanner.rs, M2 carrier-B)").
- `fetch()` hace GET :81-84; `Ok(None)` = carrier ausente/expirado (:20-21, :74-75).
- Su header :4 cita "(TTL 300s)" refiriendo al productor scanner — documentación, no escritura.

Consumidores del key (ambos fail-closed sobre ausencia/expiración):
- relays-client `submit_engine.rs:161` (GET) y :442-484 (fail-CLOSED → Dropped).
- sim-ctl `canonical_plan_consumer::fetch` — **consecuencia adicional no dicha por el
  cross-exam**: la ventana de 60 s también acota la ruta de record/replay de sim-ctl
  cuando el carrier viene de candidate_simulation.

## 3. Fix aplicado (doc-only, marcadores `// WO-02d-FIX-G2`)

1. `02d-RELAYS-CLIENT-SHARED.md` fila §4 (:249) — reescrita in situ con marcador:
   TTL NO uniforme, escritores exactos con líneas, canonical_plan_consumer reclasificado
   consumidor, ventana 60 s de la ruta candidate, ambos consumidores.
2. Ídem, nota de precisión insertada bajo el párrafo del adendum del verify
   ("productores = exactly the 3 listados") — aditiva, el texto del verificador intacto.
3. `02d-RELAYS-CLIENT-SHARED.md` **§ERRATA-03** append-only con toda la evidencia §2.
4. Board `GOAL-WORKORDERS.md` — entrada `// WO-02d-FIX-G2 APLICADA` (reglas de mesa).

## 4. Desviación del hint del charter (documentada)

Hint: "TTL 300 s (scanner, canonical_plan_consumer) / 60 s (candidate_simulation.rs:586)".
**Rechazado en su mitad consumer**: listar canonical_plan_consumer como titular del
TTL 300 reproduciría exactamente el error de clase que este fix corrige (productor vs
consumidor — es un GET, nunca escribe). El TTL 300 pertenece SOLO a scanner.rs:2458.
Cita de línea corregida: el `60` vive en candidate_simulation.rs:585, no :586.

## 5. Coordinación con fixer paralelo (WO-02d-FIX-GRAN)

- GRAN aterrizó sus ediciones §3 (granularidad db_pool/health/logging/metrics) DURANTE
  este fix (dos detecciones de archivo-cambiado-en-disco). Mis ediciones sobrevivieron
  intactas (verificado por grep post-edit); sus marcadores WO-02d-FIX-GRAN intactos.
- Numeración de erratas: GRAN referencia "§ERRATA-02" (:237/:239) que NO estaba en disco
  al iniciar mi append — usé **ERRATA-03** para mi errata y declaré la reserva de
  ERRATA-02 en su preámbulo. GRAN escribió su ERRATA-02 después (orden final en disco:
  ERRATA-01, ERRATA-03, ERRATA-02) y su cierre declara "Sin colisión con el fixer
  paralelo WO-02d-FIX-G2 (TTL §4)" — mutuo, confirmado.
- Contaminación de mi propia errata corregida ANTES de publicar el reporte: mi primer
  borrador decía que 02c "cita el carrier sin afirmar TTL" — FALSO (02c afirma "TTL 300s"
  en :30/:127/:258/:326); corregido in situ en ERRATA-03 (auto-aplicación del estándar
  §34.5.3 a mi propio texto).

## 6. Contaminación residual en archivos de otros owners (documentada, NO pisada)

- `02c-SIM-STACK-SELECTOR.md:30, :127, :258, :326` — afirma "TTL 300s" para el key
  carrier. :127 acota bien el escritor ("scanner M2 carrier-B") pero presenta 300 s
  como EL TTL del key sin registrar el segundo escritor EX 60 (mismo defecto de
  completitud corregido aquí en 02d). :258/:326 son citas exactas de la documentación
  del header canonical_plan_consumer.rs:4 (correctas como citas de doc). Owner WO-02c:
  registrado para su propio fixer, NO tocado.
- `WO-02d-verify-CROSS-EXAM.md` §3 — registro del refutador, intacto por diseño
  (su off-by-one ":586" corregido en §4 arriba).
- `WO-02d-FIX-G1-20260917.md` último bullet ("DEFECTO 2... pendiente de su propio
  fixer") — histórico, intacto; este fix lo cierra.

## 7. Verificación post-edit

- grep `TTL 300 s\)` en 02d-RELAYS-CLIENT-SHARED.md = 0 ocurrencias del claim uniforme
  (solo restan los contextos corregidos con marcador y las erratas).
- Marcadores `// WO-02d-FIX-G2` presentes en: fila §4 (:249), nota del adendum (:320-322),
  ERRATA-03 (:380-381); marcadores WO-02d-FIX-G1/GRAN intactos.
- Cero `.rs` tocados; cero git (sin commit/push/PR); cero cargo/npm/build; cero VPS;
  cero HTTP (0/5 requests).
