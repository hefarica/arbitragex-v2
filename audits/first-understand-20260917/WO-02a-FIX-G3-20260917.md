# FIXER — WO-02a-FIX-G3: errata de premisa (engines gate) + requisito sin fuente ("≥8") — 2026-09-17, gang ronda 1

> Charter G3 del cross-examiner (`WO-02a-verify-CROSS-EXAM.md` §2 R3 + tabla G2 + R4).
> Tres componentes: (i) desliz aritmético de la Corrección A ("~123.7K LOC .rs"),
> (ii) premisa falsa "TODO el árbol engines/ tras experimental-gate" (verify §3),
> (iii) reclamo "15 fichas (≥8 exigidos)" sin fuente (verify §7).
> Reglas duras: CUMPLIDO — 0 git, 0 cargo/npm/build (board WO-02 los prohíbe este run),
> 0 VPS, 0 HTTP (0/5 presupuesto), 0 mocks. Append-only sobre trabajo ajeno.

## 1. Estado inicial al aterrizar (sincronía de mesa)

- Componente (i) numérico: **YA corregido** por DOS pares antes de mi despacho
  (`WO-02a-verify-VERIFY.md` §8 "ERRATA NUMÉRICA" + sección "ERRATA NUMÉRICA
  append-only — WO-02a" E-a/E-b/E-c + bullets del board ERRATA NUMÉRICA / CONVERGENCIA
  DUAL). Mi acción: re-verificación independiente (§4.3) — coincide. Sin duplicación.
- Componentes (ii) premisa y (iii) requisito: **NO corregidos** por ningún par
  (verificado grep `experimental-gate|TODO ese árbol|≥8` en el dir; los fixers previos
  R2/S9/ERRATA-NUMERICA tocaron otras secciones). Son mi entrega propia.

## 2. Evidencia recomputada (CANONICAL_REPO, re-ejecutable)

**A. Premisa §3 — "TODO engines/ gated" es FALSO** (lectura directa engines/mod.rs completo):
- `:25-28` — `pub mod dex_engine; flashloan_engine; liquidation_engine; triangular_engine;`
  SIN gate (doc del módulo :37: "The production orchestrator uses the four stable
  engines above").
- `:31-33` — `pub mod cross_chain_bridge_engine; liquidation_snipe_engine;
  spanning_tree_engine;` SIN gate ("New engines for Task 3").
- `:38-52` — SOLO 7 módulos gated (backrun :39, cex_dex :41, spatial :43, dlp :46,
  funding_rate :48, svs :50, triangular_atomic :52; cada `pub mod` con `#[cfg(feature =
  "experimental-engines")]` en la línea previa).
- **Hallazgo propio adicional**: el cross-exam R1.1 dice "los 8 motores de :38-52" —
  MISCOUNT (son 7 módulos). Corregido en la errata para no propagarlo.
- **La conclusión de valores SOBREVIVE** (inventario exhaustivo grep `min_profit_usd`
  en src/engines/ = 22 matches):
  - 21 matches con defaults (5.0/15.0/6.0/12.0/10.0/8.0/7.0) TODOS en engines gated
    (backrun:40, cex_dex:37, dlp:75, funding_rate:45, spatial:37, svs:39,
    triangular_atomic:52 + campos/usos).
  - Único match ungated: `triangular_engine.rs:871` (`min_profit_usd: 0.01`) dentro de
    `#[cfg(test)] mod tests` (bloque :843-845, verificado por lectura directa :838-875).
  - CERO matches en los 7 módulos ungated (grep por archivo: dex 0, flashloan 0,
    liquidation 0, triangular 0 [salvo el test], cross_chain_bridge 0,
    liquidation_snipe 0, spanning_tree 0, candidate 0).
- Justificación corregida del PASS §3: NO "todo gated" sino "cero min_profit_usd
  productivo fuera de engines gated; el único ungated con valor es #[cfg(test)]".

**B. Requisito "≥8" — SIN FUENTE** (grep `exigido|≥8` en audits/first-understand-20260917
+ board leído completo): ningún charter visible ni regla del board fija mínimo de 8
fichas. Es tamaño de muestra autoimpuesto presentado como requisito externo.
**Contaminación residual de la misma fórmula (NO tocada, owner WO-02c)**:
`02c-SIM-STACK-SELECTOR.md:241` ("requisito ≥8 cumplido") y `:363` ("req ≥8"),
`WO-02c-verify-VERIFY.md:17` ("requisito ≥8 → ejecutado 18").

**C. Componente numérico — verificación independiente** (Git Bash, backend/searcher-rs):
`find src -type f -name '*.rs' | wc -l` → **178**; `find src -type f -name '*.rs'
-print0 | xargs -0 cat | wc -l` → **90,401**. Coincide con lo canónico publicado por
los pares (90,395 vía lines-per-file.txt, delta 6 = convención de conteo).

## 3. Fix aplicado

1. `WO-02a-verify-VERIFY.md` — nueva sección "## // WO-02a-FIX-G3 (2026-09-17) — ERRATA
   DE PREMISA §3 + FUENTE DE REQUISITO §7 (append-only)" al final (G3-1 premisa engines,
   G3-2 requisito ≥8 + contaminación 02c, G3-3 confirmación numérica). Cero líneas
   previas modificadas (§1-§7, §8 y E-a..E-c intactos).
2. `GOAL-WORKORDERS.md` — bullet "// WO-02a-FIX-G3" dentro de WO-02 (después de
   CONVERGENCIA DUAL): síntesis + directriz de que la cita correcta del gate engines es
   "4 estables + 3 Task-3 ungated; 7 gated :38-52".
3. NO tocados (claims de otros owners): `WO-02a-DESIGN.md` (diseñador), archivos 02c
   (contaminación ≥8 documentada, no editada), `backend/` src/ (0 mutación).

## 4. Verificación del fix

- Marcador: grep `WO-02a-FIX-G3` en WO-02a-verify-VERIFY.md = 1 sección única; en
  GOAL-WORKORDERS.md = 1 bullet único. Sin marcadores en src/ (restricción board).
- Append-only verificado: estructura del verify pre/post (§1-§7 + §8 + E-a..E-c
  byte-idénticos; el diff neto es solo la sección nueva al final).
- 100% de file:line citadas re-abiertas ANTES de escribir (engines/mod.rs completo,
  triangular_engine.rs:838-875, greps min_profit_usd exhaustivos, find/wc).
- Sin cargo/tsc aplicable (fix documental; el board prohíbe builds este run).
- Auto-corrección durante el fix: mi primer borrador de la errata decía "8 módulos
  gated" (contagiado del cross-exam) — detectado al re-contar mod.rs y corregido a 7
  ANTES de cerrar (queda documentado dentro de la propia errata).

## 5. Sincronía de mesa redonda

- Construye sobre: `WO-02a-verify-CROSS-EXAM.md` (R3/G2/R4 origen del charter; su R1.1
  "8 motores" corregido aquí con evidencia), `WO-02a-verify-VERIFY.md` §8 + E-a..E-c
  (fixers numéricos previos — confirmados, no duplicados), GOAL-WORKORDERS.md bullets
  ERRATA NUMÉRICA / CONVERGENCIA DUAL.
- Para WO-02c: queda PENDIENTE de su owner la misma corrección del "≥8" en
  02c-SIM-STACK-SELECTOR.md:241/:363 y WO-02c-verify-VERIFY.md:17 (documentada, no pisada).
- Para los gates 1-8 (WO-06): al citar el gate de engines, usar "7 módulos gated en
  engines/mod.rs:38-52; 4 estables :25-28 + 3 Task-3 :31-33 compilan sin gate". Al citar
  muestreo del verify: "15 fichas (autoimpuesto)", no "≥8 exigidos".
- Ninguna conclusión estructural cambia: el PASS RULE 00 §3 sobrevive con justificación
  corregida; el PASS 15/15 no depende del número 8; los conteos LOC ya eran canónicos.

Clasificación de evidencia: CANONICAL_REPO (file:line + comandos reproducibles).
