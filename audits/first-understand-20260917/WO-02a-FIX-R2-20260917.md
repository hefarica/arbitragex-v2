# WO-02a-FIX-R2 — Errata material: fuente fantasma `configs/router_kinds.json` (§7) y punto de propagación inexistente (D-1) — 2026-09-17, FIXER gang ronda 1

> Charter: corregir la errata del cross-examiner — WO-02a repitió el doc-comment de
> impact_index.rs como fuente activa y D-1 afirmó que "configs/router_kinds.json ya tiene
> la dirección por dcfe890c". Entregable: errata append-only en `WO-02a-DESIGN.md`
> (§ "ERRATA MATERIAL (WO-02a-FIX-R2...)", líneas 468+) que marca la fuente 4 como
> MUERTA/NO-IMPLEMENTADA y reescribe D-1 (D-1-rev2): la propagación PancakeV3→
> ProtocolType::V3 vive donde el decoder asigna `protocol_type` a las piernas, con el
> gate de verificación del decoder mantenido en `calldata/`.
> Restricciones cumplidas: 0 git/cargo/VPS/HTTP; 0 edits a src/ (oleada read-only,
> diseños gated operador); markdown-only.

## 1. Cambios aplicados

1. `WO-02a-DESIGN.md` — errata append-only al final (E-1/E-2/E-3). Ninguna línea previa
   modificada ni borrada; el D-1 original queda intacto y marcado SUPERSEDO por referencia.
2. `GOAL-WORKORDERS.md` — nota append en la entrada ERRATA-WO-02a del board marcando esta
   errata aplicada (sincronía de mesa).
3. NO se tocó: `WO-02a-verify-VERIFY.md` (archivo del verificador — su fila §7 "router_kinds.json
   PASS" queda refutada por E-1; adendum a decisión de su owner/orquestador), ni
   `backend/searcher-rs/src/impact_index.rs` (ver §4), ni `route_intent.rs` (diff del operador).

## 2. Evidencia recomputada (todo CANONICAL_REPO, re-ejecutable)

**A. configs/router_kinds.json NO existe** — `find . -name "router_kinds.json"` (raíz repo,
2026-09-17) = 0 resultados. Única mención del archivo en todo el árbol: el doc-comment
impact_index.rs:21-22. Es fuente fantasma DOCUMENTAL: el comentario del propio código
describe una fuente jamás implementada.

**B. router_to_protocol = campo muerto** — grep repo `router_to_protocol` = 3 hits exactos:
- impact_index.rs:21 (doc-comment, la fuente del contagio);
- :221-222 `#[allow(dead_code)] router_to_protocol: HashMap<Address, RouterKind>`;
- :244 inicialización vacía `HashMap::new()` en `empty()`.
CERO escritores con datos, CERO lectores. `resolve()` (:452-473) y `resolve_leg()`
(:476-519) no consultan router alguno: `ImpactSet.impacted_protocols` sale del registro de
Variedades (`pool_ref.protocol_type`, :503).

**C. Bonus (hallazgo nuevo para la mesa)** — fuente 5 `selector_to_decoder` (:226-227)
TAMBIÉN muerta: `#[allow(dead_code)]`, poblada en :245 por `build_selector_table()`
(:529-564), 0 lectores. De las 5 "fuentes de boot" del doc :12-23, solo 1 y 2 construyen
algo real; 3 = vacía by-design (Phase 11 R8); 4 y 5 = dead_code.

**D. Mecanismo real del hueco Pancake (corrige §3 del DESIGN)** — el swap muere en el
DECODER, no llega degradado al orchestrator: route_decoder.rs:83-87 → calldata::decode
dispatch (calldata/mod.rs:101-108) → `PancakeV3` cae en `_ => Err(UnknownRouter)` (:107)
→ `Ok(vec![])` = CERO RouteIntents. calldata/ contiene SOLO univ2/univ3/universal_router
(ls directo): no existe decoder Pancake — el "HYPOTHESIS" del D-1 original es GAP CONFIRMADO.

**E. dcfe890c NO tocó ninguna config de routers** — `git show --stat dcfe890c` =
`backend/shared-rs/src/chains.rs` + `backend/sim-ctl/src/tx_builder.rs` únicamente.
Coincide con la errata de procedencia del board (diff route_intent.rs = huérfano
working-tree, no commit).

**F. Cadena real router→protocolo** (donde D-1-rev2 ubica la propagación): catálogo
chains.rs:415-416 → route_decoder.rs:84 → dispatch calldata/mod.rs:101-108 → sub-decoder
asigna `DecodedSwap.protocol_type` (calldata/mod.rs:74; univ3.rs:116 para V3) →
route_decoder.rs:264 `protocol_type: decoded.protocol_type` → `RouteIntentLeg.protocol_type`
(consumidores vivos: candidate_simulation.rs:189 parse_dex_kind; cartridge_boot.rs:240,
290-325, 621).

## 3. Contenido de la errata (resumen ejecutivo para la mesa)

- **E-1**: fuente 4 de §7 marcada MUERTA/NO-IMPLEMENTADA (falso doble: archivo inexistente
  + campo dead_code). Corrección de cita: el puntero §3 "impact_index.rs:31
  router_to_protocol" apuntaba al `use` (:31-32); el campo vive en :221-222.
- **E-2**: corrección de mecanismo del hueco (muerte en decoder, no degradación en
  orchestrator).
- **E-3 / D-1-rev2**: (1) nuevo `calldata/pancake.rs` espejo del encoder canónico de
  tx_builder.rs (7-tuple sin deadline, selector computado, `protocol_type: ProtocolType::V3`,
  fees pips raw); (2) brazo `RouterKind::PancakeV3` en calldata/mod.rs:101-108 (sin él nada
  tiene efecto); (3) la promoción de enum de route_intent.rs del D-1 original SIGUE válida;
  (4) impact_index SIN cambios — el "gate de impact_index" del D-1 original queda OBSOLETO.
  Gates: tests del decoder en calldata/ (protocol_type==V3, pips raw, 7-tuple) +
  round-trip route_decoder (legs[0].protocol_type==V3, router_kind==PancakeV3), análogos a
  univ3.rs:415-482 y route_decoder.rs:501/630. Todo NO aplicado, gated operador.

## 4. Decisión de alcance (documentada, no silenciosa)

El doc-comment impact_index.rs:21-22 es la RAÍZ del contagio (dos agentes repitieron la
fuente fantasma). Su corrección es un diff de comentario (cero comportamiento), pero esta
oleada es read-only para src/ (restricción board WO-02) y todo diseño está gated operador
(§37, D-1..D-3 NO aplicados). Decisión: NO editar src/; la corrección del doc-comment
queda RECOMENDADA en E-1 como paso inicial de D-1-rev2 cuando el operador lo despache.

## 5. Verificación

- Markdown-only: no aplica cargo/tsc (board WO-02 los prohíbe este run).
- Marcador: grep `WO-02a-FIX-R2` en WO-02a-DESIGN.md = presente (cabecera de errata).
- 100% de file:line citadas re-abiertas y confirmadas ANTES de escribir (find, greps,
  lecturas directas de impact_index.rs:1-80/200-320/452-564, calldata/mod.rs completo,
  route_decoder.rs:60-190, route_intent.rs:245-280, git show --stat dcfe890c).
- Honestidad concurrente: el archivo cambió en disco durante mi edición (par FIX-R1 /
  orquestador); re-verifiqué estructura post-edit: mi errata quedó única al final (:468+),
  sin pisar §9.1 de R1 ni secciones del verify. Cero conflictos de claims de archivo.

## 6. Sincronía de mesa redonda

- Construye sobre: GOAL-WORKORDERS.md (entrada ERRATA-WO-02a — cierra las erratas
  "PENDIENTES" de §3/§10-D1 para esta mitad), WO-02a-verify-CROSS-EXAM.md (su :35 repite
  "router_kinds.json" — contaminación residual ya anotada por el board, NO tocada aquí,
  archivo de otro owner), WO-02a-FIX-S9-20260917.md (R1, §9 — sin solape).
- Para WO-02b/02c/02d: sin dependencias — este fix es searcher-rs local.
- Para el operador: D-1-rev2 es el diseño canónico si se decide cerrar el hueco Pancake;
  el prerequisito (decoder inexistente) está confirmado, ya no es HYPOTHESIS.
