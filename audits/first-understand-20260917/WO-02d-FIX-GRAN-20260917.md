# WO-02d-FIX-GRAN — granularidad de prueba §3 shared-rs (db_pool/health/logging/metrics)

> kind: fixer · gang ronda 1 · 2026-09-17 · READ-ONLY (§32/§33), cero cargo/npm/build
> (restricción board WO-02), cero git, cero VPS, cero HTTP (0/5 requests).

## 1. Charter

Cerrar la observación 4.1 del cross-exam (`WO-02d-verify-CROSS-EXAM.md` §4.1):
"Granularidad de prueba desigual en §3 shared-rs: varias filas citan solo ':1-8'
(headers) — p.ej. `metrics.rs` (481 LOC) fichado con prueba ':1-8 c/u'. El gate del
board exige 'línea exacta'". Hint: fortalecer citas de metrics.rs vía
init_metrics/metrics_handler en lib.rs re-exports, o marcar GAP de prueba.

## 2. Cambios aplicados (todos marcados `// WO-02d-FIX-GRAN (2026-09-17)`)

Archivo: `02d-RELAYS-CLIENT-SHARED.md` (superficie WO-02d).

1. **Fila agrupada §3 (:233 original) DESGLOSADA en 4 filas** con citas file:line
   concretas, derivadas de lectura COMPLETA de los 4 fuentes (no grep suelto):

   | Módulo | Citas nuevas (re-verificadas) |
   |---|---|
   | `metrics.rs` (481) | `REGISTRY` :9 · `init_metrics()` :431-464 (**30** collectors `Lazy` forzados + `SERVICE_UP.set(1)` :463) · `metrics_handler()` :466-481 (gather→TextEncoder→200; err→500 :470-475) · `record_inclusion()` :419-429 · 11 bloques de familias por rango exacto: HTTP :11-36, OPPORTUNITIES :38-49, SIMULATIONS :51-62, SIM_FUNDING (SIM-FUND-01) :64-80, EXECUTIONS :82-90, KILLSWITCH gauge :92-96, searcher :104-180, RPC failover (G-RPC-1) :182-261, cred rotation (F4) :263-296, bundle inclusion (N7) :298-334, SIMWIRE-02 PEL :346-409 |
   | `db_pool.rs` (89) | `PoolConfig::from_env()` :39-67 (5 env vars; defaults 8/1/5 s/600 s/1800 s) · `options_with_timeouts()` :75-82 · `connect_pool()` :86-89 |
   | `health.rs` (58) | `ServiceInfo` :8-26 · `build_health_router()` :36-58 (`/health` JSON ok+uptime_s :38-56; `/metrics` :57 despacha a `crate::metrics::metrics_handler`) |
   | `logging.rs` (28) | `init_tracing()` :7-28 (EnvFilter :8; JSON layer :10-18; boot event `service.boot` :26) |

2. **Hint del cross-examiner CONFIRMADO con evidencia**: re-exports
   `shared-rs/src/lib.rs:38` (db_pool), `:39` (health), `:41` (logging), `:42`
   (metrics: `init_metrics, metrics_handler`); consumo vivo del terminus:
   `relays-client/src/main.rs:42,44-45` (imports), `:117` (`init_tracing`), `:118`
   (`init_metrics`), `:455` (`build_health_router(...).merge(exec_router)`).

3. **GAP de prueba declarado (fail-honest, R8/RULE 00)**: `grep -n "cfg(test)`
   metrics.rs db_pool.rs health.rs logging.rs` = **0 hits**. La columna Prueba cita
   líneas de misión exactas; la cobertura por test unitario NO existe y queda
   marcada **GAP** en cada fila — se cumple la disyuntiva del gate del board
   ("línea exacta O marcada GAP": ahora ambas mitades honestas).

4. **§ERRATA-02 append-only** al final del entregable documentando todo lo anterior.

## 3. Hallazgo NUEVO colateral — doc-drift `db_pool.rs:14-16`

El doc-comment del módulo afirma: "Logging: on success, emits `db.connected` [...]
On timeout/error, emits `db.connect_failed`". **El módulo contiene CERO llamadas de
logging** (0 hits `tracing`/`info!`/`warn!` en el archivo). Los eventos R6 los emiten
los CALLERS:

| Emisor real | Evidencia |
|---|---|
| relays-client | `src/main.rs:276` (`db.connected`), `:281` (`db.connect_failed`) |
| searcher-rs | `src/main.rs:532`, `:544` |
| sim-ctl | `src/main.rs:707-712` — con NOMBRES propios (`sim.db_connected`/`sim.db_connect_failed`): drift de nomenclatura adicional vs R6 |
| token-enricher | `src/main.rs:464` (`enricher.db_connected`) |

Impacto: documental. R6 ("log verificable `db.connected`") se cumple de facto en
relays-client/searcher; inconsistente en sim-ctl/token-enricher. Corrección del
doc-comment del `.rs` = **operator-gated, NO aplicada** (cero código en este fix).

## 4. Auto-corrección in-fix (RULE 00 aplicada a mí mismo)

La primera redacción de la fila metrics.rs decía "29 collectors"; recompte exacto
(`grep -c "pub static" metrics.rs` = 31, incl. `REGISTRY` :9 → 30 collectors;
`sed -n '431,464p' | grep -c "let _ = &\*"` = 30) → corregido a **30** in situ
antes del cierre. Registrado para la mesa: incluso el fixer verifica sus propias
cifras.

## 5. Conflictos de claims de archivo / no-regresión

- Archivos tocados: `02d-RELAYS-CLIENT-SHARED.md` (fila §3 + §ERRATA-02) y
  `GOAL-WORKORDERS.md` (entrada append-only en Reglas de la mesa). **Cero archivos
  de otros WO pisados.**
- Colisión detectada y resuelta SIN pérdida: un fixer paralelo (WO-02d-FIX-G2, TTL
  §4) editó el mismo archivo durante mi ventana. Ediciones disjuntas (mi fila :233-242,
  su fila :249 + :320 + su errata); marcadores de ambos intactos post-edit
  (verificado con grep). Nota de orden: su §ERRATA-03 (TTL) quedó insertada ANTES de
  mi §ERRATA-02 en el archivo (secuencia física 01→03→02) — coexistencia append-only,
  sin renumerar contenido ajeno.
- Fuera de charter, documentado NO tocado: filas `cred_rotation.rs` (:1-8) y
  `tokens.rs` (:1-8), y las citas ":1-N" de módulos puros pequeños (flashloan_math,
  oracle_snapshot, candidates, etc.) — el charter nombraba SOLO metrics.rs y el grupo
  db_pool/health/logging. Si la mesa quiere granularidad total de §3, es otro fix.
- `git status` de fuentes Rust: `backend/shared-rs/` y `backend/relays-client/` sin
  cambios (los M preexistentes del snapshot — route_intent.rs, settings, CLAUDE.md —
  ajenos a este WO, no tocados).

## 6. Verificación

- Post-edit grep de anclas: `WO-02d-FIX-GRAN` presente :233/:237/:380 (entregable);
  4 filas nuevas renderizan la tabla §3 con conteo 19 filas → 22 filas post-split
  (19 original con la agrupada como 1 → ahora 22; cobertura de archivos SIN cambio:
  los mismos 23/24 src, solo granularidad).
- Citas byte-exactas re-verificadas contra lectura completa de los 4 fuentes
  (Read tool, no grep parcial).
- Cero cargo/npm/build ejecutados (restricción board WO-02 respetada).
