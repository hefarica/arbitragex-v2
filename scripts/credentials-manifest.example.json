{
  "_comment": "RunFullSyncCycle manifest TEMPLATE — placeholders only, NEVER real values. Copy OUTSIDE the repo (env ARBX_CRED_MANIFEST points at it). For rpc_http/rpc_ws the CSV order IS the array order: first entry = titular, the rest = fallbacks in rotation priority.",
  "items": [
    {
      "provider": "rpc_http",
      "scope": "chain:1",
      "secret_value": "titular=https://your-primary-rpc.example/v2/YOUR_KEY,fallback1=https://fallback-one.example,publicnode=https://ethereum-rpc.publicnode.com"
    },
    {
      "provider": "rpc_ws",
      "scope": "chain:1",
      "secret_value": "titular=wss://your-primary-ws.example/v2/YOUR_KEY,drpc=wss://eth.drpc.org"
    },
    {
      "provider": "coingecko_pro",
      "scope": "global",
      "secret_value": "CG-YOUR_PRO_KEY"
    }
  ]
}
