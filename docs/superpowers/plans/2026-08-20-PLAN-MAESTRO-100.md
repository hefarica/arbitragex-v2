# PLAN MAESTRO — ArbitrageX 61% → 100%

> Generado 2026-08-20 tras la sesión de cierre A.4. Integra: (a) auditoría global
> externa (61% madurez, ±5pts), (b) informe de cadena de fallos producción → Edge/API
> → WS → mapper → economía → card, (c) auditoría de los 20 PRs abiertos (workflow de
> 10 agentes), (d) cierres verificados de esta sesión. Regla §37: todo cambio con ID,
> un PR = un ID, evidencia L4 por fase.

## 0. Estado base (post-sesión 2026-08-20)

| Ítem | Antes | Ahora |
|---|---|---|
| A.4 fork validation | BLOCKED (nunca ejecutada) | **CLOSED**: `a4_state=A4_PASSED` prod, fila `gate_c_validation` id=1, outcome profundo `multistep_gross_spread_non_positive` |
| Blocker readiness a4 | presente | **REMOVIDO** (#435); endpoint vivo ya no lo lista |
| WS-POLL-1 (socket.io→edge 404 + gate admin) | roto | **MERGED #437** (rewrite a api-server + room pública) |
| Deploy truth (G4) | drift (api-server viejo post-deploy fallido) | **RESTAURADO**: VPS `c4ea8aaa` = main, deploy success |
| Rojos CI unit_tests/dep_tree | determinísticos | **CAUSA RAÍZ ARREGLADA**: `ARBX_ADMIN_TOKEN` ×2 en .env (dedup); hardening action.yml pendiente |
| G-SIM-1 fork_suite | muerta (SIGABRT stack.rs:206) | RUSTFLAGS (#436) + spawn_blocking (#439 en cascada) → `FORK_SUITE_OUTCOME=PASS` verificado VPS bloque 25.79M |
| Race deploy G-CI-2 | viva en main | fix en **#429** (cascada armada) |

## 1. Cascada en vuelo (auto-merge armado, orden)

1. **#439** G-SIM-1 spawn_blocking (merge_ready) → al aterrizar: dispatch `sim-fork-evidence.yml` en main → `FORK_SUITE_OUTCOME=PASS` + POST evidencia (registry ya sano).
2. **#438** EDGE-HARD-1 (lint fixeado en branch por esta sesión).
3. **#433** parity G03/G04 (test propio actualizado por esta sesión).
4. **#429** G-CI-2 race deploy (merge_ready, branch actualizada).

Cada merge dispara auto-deploy; si la race muerde otra vez → deploy manual (runbook probado) + la race queda muerta definitivamente tras #429.

## 2. Disposición de los 20 PRs auditados

**Merge-ready (update branch + auto-merge, en orden de riesgo creciente):**
#397 (rust-cache, 0 behind) → #304 (candle-core) → #307 (rust-minor) → #377 (npm-minor) → #376 (cred FASE 3b) → #422 (G-PRICE-5+G-PRICE-2).

**Fix-then-merge (fixes EN sus branches, trivial → sustantivo):**
- #430: 2 vars no usadas en `op_32_vpin.rs` (clippy -D) → prefijo `_`.
- #425: clippy redundant-map → merge listo tras fix.
- #401: `forge fmt` en su script nuevo → luego merge (cierra blocker §IV A2).
- #392 (RU-5 wave A): rebase + CI rerun.
- #417 (rpc-bridge): 7 errores rustc (Debug derives, [u8]→Address, E0283) — sustantivo.
- #303 (redis 1.x): major breaking — actualizar call-sites o cerrar y reabrir con plan.
- #302 (statrs): stale 46 — rebase tras los otros deps.
- #367 (FREEZE-02): CONFLICTING y mayormente superseded por #368/#373 → **cerrar** conservando el guardian si no aplica.

**Cerrados/integrados**: ninguno adicional (todos verificados no-integrados salvo señal contraria).

## 3. Fases 61% → 100% (escalera del informe, con IDs y criterios L4)

### FASE 1 — Transporte live + deploy truth (61→66%) · ~2 días
- [x] WS-POLL-1 (#437 MERGED).
- [x] Deploy truth restaurado; #429 mata la race para siempre.
- [ ] **P1 SSR-EDGE-AUTH** (nuevo, ID `SSR-EDGE-TOKEN-1`): `/opportunities/page.tsx` (y todo SSR fetch) debe usar `api-client` con `x-arbx-edge-token` — hoy bypassea y comparte bucket "anon" (riesgo auto-429). L4: header presente en logs edge desde SSR, 0 429 en ventana de prueba.
- [ ] **Hardening action.yml** (ID `G-SIM-1-POST`): `| head -1` en grep de token + retry ssh (exit 7). L4: workflow verde 3 corridas seguidas.
- [ ] Verificación post-#437 en vivo: `edge:8787/socket.io` → 200 vía rewrite; feed `CONNECTED` sin admin token. L4: Playwright probe.

### FASE 2 — Route discovery crown jewel (66→72%) · ~1 semana
- [ ] #411 DeferNeverDrop: limpiar artefactos del branch (target-probe/, .cargo) → CI → merge. L4: `/routes/discovery` vivo sin "routes capped", contador `routes_deferred` > 0, cursor avanzando entre ticks.
- [ ] #430 (VPIN + tip optimizer + split-route convex) tras clippy fix — economía de ruta.
- [ ] Route→pool incremental + podas core/hub + hop yield (IDs del doctrine ROADS_CROWN_JEWEL).

### FASE 3 — 115 cartuchos route-based E2E (72→80%) · ~2 semanas
- [ ] #392 (RU-5 wave A) + waves B–E restantes con matemática real.
- [ ] #433 ya en cascada (6/10 opps reviven en label mapping).
- [ ] Cadena completa por familia G01–G04: detector→candidate→sizing→sim→ranking→telemetry, sin stubs. L4: `By Strategy` vivo con señales > 0 por familia; funnel born/died por (hop_tier, gate, razón, financing_mode) — nada muere en silencio.
- [ ] #401 (sim-ctl Anvil executor) cierra §IV A2 del memory de blockers.

### FASE 4 — Engines off-graph (80→90%) · ~3-4 semanas
- [ ] derivatives (30) + cross-domain (30) + credit/liquidations (25) + intents (20) + CEX (14): cada uno necesita feed propio + detector honesto (no YAML-stub). Orden por valor: intents→liquidations→derivatives→cross-domain→CEX.
- [ ] NFT (18) + prediction (12): decidir explícitamente (disabled honesto vale — quitar del denominador de "soportadas" o implementar).

### FASE 5 — Economía final (90→94%)
- [ ] #422 (G-PRICE-5 CB + G-PRICE-2 repricing) merge.
- [ ] #425 (G-PRICE-3 Chainlink sub-2s) tras clippy.
- [ ] Per-route financing, P(inclusion), post-bribe EV, calibración realized-vs-predicted, VPIN en vivo.

### FASE 6 — Sim + ejecución (94→97%)
- [ ] SIM_SUCCESS completo (M5 testnet: FLE+AE desplegados en Sepolia, flash round-trip real en fork de testnet).
- [ ] Hazard documentado `read_balance` caller=account (disparará 3607 con FLE real — usar caller code-less en view calls).
- [ ] Private relay/bundles/inclusion/reconciliation (relays-client ya UP; wiring gated §34).

### FASE 7 — LIVE gates (97→100%)
- [ ] A.5 paper-shadow ≥7 días continuos + calibración (bloqueado HOY, correcto).
- [ ] A.6–A.9 (circuit breakers calibrados, relay privado, confidence wired, GO/NO-GO formal).
- [ ] Los 3 gates §34.3 (paper-first, simulation-mandatory, risk-limits) + autorización operadora explícita. **Capital real jamás sin esto.**

## 4. Deuda transversal (paralela a todo)
- Históricos NULL (G-ECON-2 era): política **D+B** del informe — superficies separadas historical/live + replay/backfill de las viables.
- Drift `.env` VPS: los dupes (ARBX_ADMIN_TOKEN fue ×2; FLASHLOAN_EXECUTOR_1 ×2) — audit completo con `sort | uniq -d` mensual.
- Dependabot majors (#303 redis, #304 candle): ventana dedicada, no de paso.
- La matriz 264: mantener `capability_matrix.json` + `route_applicability.yaml` sincronizados con cada wave (anti-engaño del 43.6% route-based).

## 5. Regla de oro (del informe, suscrita)
100% = cada estrategia soportada con la cadena DATA→DETECTOR→…→TELEMETRY sin ninguna flecha `stub/mock/TODO/disconnected/silent-fail`, y `main == VPS == UI` siempre. Los porcentajes avanza al cerrar **cadenas E2E**, no páginas.
